# 事实抽取实验总路牌 R11｜EX 收口，下一项只盘点 BG 材料

✅ 修正后的固定对比例已经真实跑完，但没有帮助。无示例的 `EX-0` 继续保留：`93 TP / 154 FP / 187 FN / F1 0.352941`。加一组对比例的 `EX-1` 只有 `69 TP / 245 FP / 211 FN / F1 0.232323`，召回率也从 `0.332143` 降到 `0.246429`。

这次不是拿旧的短而过密示例硬跑。A、B 两个成员已经按外部调查结论修成 311／321 个 Unicode 字符、各 5 条事实，并加入相同的正常叙事载体，状态边界也交叉排列。模型仍明显多抽，所以 EX 家族到此关闭：不加第三个示例，不做检索示例，不重跑，也不进入示例训练 2×2。

当前本机 DEV24 守擂结构不变：未微调本地 4B，目标左右每边最多 180 个 Unicode 字符的只读背景，`OUT2-IDLIST`，完整自然责任段 `620—923` 字，使用最短的 `RULE0`，并且不放固定示例。它仍只是本机开发集上的临时守擂项，不是生产、Mini 或通用最优结论。

下一家族按既有顺序登记为 `BG`，但现在只做本地只读的来源盘点：确认 DEV24 是否真的有足够的、带来源和时间截点的题外背景，可以组成“相关确认背景”和“等长度中性背景”。背景永远不能当作当前题的事实证据；冲突背景只可作压力材料，永不训练。材料不够就停，不能现编背景凑数。

R11 不授权 BG 请求渲染、模型加载、推理、训练、API、Notion、Git 或生产晋级。R10、EX 准备包、运行与评分证据全部保持原字节。

机器状态看 [DECISION_REGISTRY.json](DECISION_REGISTRY.json)，EX 结果与证据看 [EX_RESULT_TICKET.json](EX_RESULT_TICKET.json)。

来源：Codex
