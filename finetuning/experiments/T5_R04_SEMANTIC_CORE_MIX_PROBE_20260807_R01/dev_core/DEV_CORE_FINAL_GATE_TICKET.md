# DEV_CORE／CORE gold 阶段票

✅ 结论：DEV_CORE 的机械构造已经通过，A_CORE 与 C2_CORE 做到了“同一批正文、同一批语义答案”。当前停在阶段票，尚未运行 Z00／Z01，尚未训练。

状态建议：`PASS_DEV_CORE_MECHANICAL_AND_SINGLE_PASS_SEMANTIC_CANDIDATE_PENDING_CZ_ACCEPTANCE`

## 这次实际做成了什么

| 项目 | 读数 |
|---|---:|
| 题目窗口 | 48 |
| 小说／作者 | 31／31 |
| 来源章节 | 48 |
| 同一本书的双窗口 | 17 本，且来自不同章节 |
| 核心事实 | 95 |
| 自然空答案窗口 | 2 |
| A／C2 同源 | 48／48 |
| A／C2 共用同一 CORE gold | 48／48 |
| 为某一种表示单独改题 | 0 |

选题规则在读任何金标和旧模型输出前冻结。R02 两次独立构建逐字节一致；共享素材台账中每本、每位作者最多 2 个窗口，没有超过全局 4 段上限。

CORE gold 只保存 `fact_sentences`，没有 evidence、status、speaker 或 B/T 编号。48 题已经完成一次本地全文精读；这是一份可审候选，不冒充已经经过第二位审查者的最终金标。

## 变量有没有拆干净

当前主对照已经拆出 marker burden：

- Z00：原文不插编号，只输出核心事实；
- Z01：同一正文插入冻结的 B/T 编号，仍然只输出同一组核心事实。

它们的 case 顺序、正文和 95 条语义答案一致，唯一计划变量是题面编号。

引用负担和跨单元负担目前只作为子臂：

- Z02：同样带编号，但再要求 `evidence_ids`。题面已机械生成，引用位置答案尚未生成；
- Z03U／Z03X：同样带编号，比较自然落在一个单元与自然跨两个单元。还缺独立位置审计和匹配成卷，因此没有伪造一个“已完成”状态。

## 编号带来了多少输入负担

用冻结 Qwen3-4B 的本地 tokenizer 和 chat template 计数，没有运行模型：

| 题臂 | 输入 token 中位数 | 平均数 | 最大值 |
|---|---:|---:|---:|
| Z00 无编号、只答事实 | 765.5 | 753.38 | 848 |
| Z01 带编号、只答事实 | 894.5 | 883.69 | 978 |
| Z02 带编号、还要引用 | 906.5 | 895.69 | 990 |

编号让每题输入平均增加 130.31 token；Z02 的引用说明再固定增加 12 token。每题可见编号为 23～28 个，平均 25.69 个。这里仅说明题面负担，不预判 C2 一定更差；Z00／Z01 正是要测这点。

token 计数脚本连跑两次完全一致，回执 SHA-256：

`f3bd33b1637d4467c4c3190985295a110543621ab2e18019d1a0999e744f3cb7`

## 评分和训练边界

后续若获准运行 Z00／Z01，沿用冻结 EVALUATOR_R02 的分层原则，分别报：

- raw／可恢复语义；
- 结构化语义；
- 端到端可用；
- schema；
- 复读；
- 截断／正常完成；
- token 成本。

不生成一个能互相抵消问题的总分。语义正确仍进入人工盲审队列，机器精确匹配不冒充语义 F1。

任何新 SFT 都被独立训练管线硬门锁住。本票没有授权模型推理、训练、API、Notion 或 Git 操作。

## 核验指纹

- DEV_CORE 封版回执：`d9140332229274f6be0a3c40c0d00915e13eec79a4bcbcc5563bfafd483979df`
- 共同 CORE gold：`90f68035708aa8dbfba609799a49f655a45c0cfb56e9af4e4c3085257a0e8494`
- A_CORE 题面：`8414eda33f94a84bfa1f8ecffc3d878dd2816a2fa7754705606b49332cb173d2`
- C2_CORE 题面：`3c94ccdc6d41efe5305e5105e062793fd3b3bb142938bd82183790da9f2ec665`

👉 下一停点：CZ 审收本票。通过后才运行 Z00／Z01；Z00／Z01 跑完也不会自动开训练。

来源：Codex
