# 事实抽取实验总路牌 R03｜只新增两类设计登记

✅ R03 继承 R02 的 19 个抽取家族、旧结果、状态和 READ 当前队列，**不改写 R02 历史，也不改变 TRAIN72 → READ 资格路线**。

本版只新增两块登记：

- `CONSUME`：抽取之后的“下游承接”家族组，一个总家族下面有 DOSE／SELECT／ROUTE／CRAFT／MODEL 五个子家族。它不是第 20 个抽取家族。
- `EDGE`：抽取之后的两阶段因果边后处理家族。边是可重建的派生视图，不进入事实真值层。

两块当前都只叫“已登记、待前置”，机器状态是 `DESIGN_REGISTERED_NOT_RUN`。这里没有排出立即运行顺序，没有 API 预算，没有训练或推理授权，也没有创建运行器、数据包或结果票。

## 当前仍按 R02 走

- 抽取侧 19 家族的机器定义、状态、旧别名和旧结果仍以 parent R02 为准。
- READ 仍是当前抽取侧队列；TRAIN72 教材／合同与 READ 赢家候选先稳定，再考虑后处理和下游承接。
- `CONSUME` 和 `EDGE` 的登记不占用当前 4B 对照训练格，也不打断 READ。

## 以后若要开跑

- `CONSUME` 先做 P0 管线小跑和 P1 裁判校准；任何商用 API 次数、金额和模型版本都要另报 CZ。
- `EDGE` 先做 P0 表示／候选对小跑和 P1 独立双盲 Gold／裁判校准；若用商用 API，同样另报 CZ。
- 登记只说明“依赖满足后值得测”，不等于执行权。

父 registry：`../T5_R04_FACT_EXTRACTION_EXPERIMENT_ROADMAP_20260809_R02/FAMILY_REGISTRY.json`，SHA-256 `1032f11162508f5b1747c606d932671d157eaadfdd02bf7f154e65ba78799134`。

来源：Codex
