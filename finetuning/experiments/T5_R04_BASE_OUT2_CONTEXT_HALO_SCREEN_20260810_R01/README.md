# Base＋OUT2 上下文窗口 Demo｜只准备

✅ 这个包只准备一件事：固定现在的 OUT2-IDLIST，看目标段左右各给多少上下文更合适。

- H180 是已经跑过的 READ2，直接复用，不重跑。
- H120 和 H60 是未来两格新推理，各24题，合计48题。
- `180 / 120 / 60` 都是“目标段左边最多 K 字，右边也最多 K 字”；章首或章尾不足就照实保留，不补。

H120/H60 只从真正跑过的 H180＋OUT2 题面向内切 Unicode 码点。不吸附句号或换行，不补段落，也不改 system、负责段、Txx 允许表、Gold、Prompt 事实合同或解码。切点若碰到 CRLF、组合字符、变体符或 ZWJ，runner 会直接停，不自动挪位。

scorer 先做机械门：JSON、证据 Txx、复读、触顶和重复事实必须干净；Schema 只照实报告，留到 F1 接近时才比。机械失败的新格不进盲审。新事实才进匿名队列，已有同 case＋同 fact SHA 的两批冻结裁决直接复用。

选择时把 H180 当守擂基线。新格与最好 F1 差不到 `0.02` 时，先看 Schema 合法题数，仍相同就选更短的 halo。两个新格都过不了机械门，就保留 H180。这只是未微调本地 4B＋OUT2＋REAL24 的本机 Demo，不是训练、Mini 或生产结论。

当前状态是 `PREPARED_NOT_AUTHORIZED_NOT_RUN`。runner 没有精确执行票时，会在创建 run 目录和加载模型前硬停。

具体冻结身份和门槛看 [SPEC.json](SPEC.json)。

来源：Codex
