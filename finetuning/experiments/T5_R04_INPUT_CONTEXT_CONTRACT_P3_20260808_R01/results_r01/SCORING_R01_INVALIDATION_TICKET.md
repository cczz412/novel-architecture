# P3 评分 R01 作废票

R01 的事实、格式、停止和配对指标计算无漂移，但 C4 的“只读前态泄漏”诊断少计了 1 案：只按新裁决标签识别了 `MICRO24B-S23`，没有把复用旧裁决的 `MICRO24B-S16` 结合其 `B01` 输出重新归入本轮输入合同诊断。

修法只改诊断归类：凡 C4 输出引用不属于负责区 `Txx` 的 `B01`，均登记为只读前态泄漏。R02 必须得到 2 案：`MICRO24B-S16`、`MICRO24B-S23`。旧 R01 留档，不作为交件真值。

来源：Codex
