# WO-01 短片段责任范围三臂 Demo R02 结果票

状态：`PASS_ZERO_TRAINING_PRESCREEN_COMPLETE_NOT_LORA_RESULT`

✅ 现成 MLX 环境复用成功。每臂前 2 题的 6 份烟雾输出通过，随后完整完成 3×24＝72 份输出。72/72 都正常结束，0 触顶、0 重复事实、0 自动重试。

## 三臂结果

| 可读范围 | 可恢复语义 Precision | 可恢复语义 Recall | 可恢复语义 F1 | 完整结构下语义 F1 | Schema 合法 |
|---|---:|---:|---:|---:|---:|
| 只给目标区 | 85.19% | 47.92% | 61.33% | 37.50% | 21/24 |
| 固定 8 字符邻域 | 90.32% | 58.33% | 70.89% | 38.10% | 19/24 |
| 完整当前窗口 | 81.08% | 62.50% | 70.59% | 51.35% | 20/24 |

主指标改为：只要整段 JSON 的根和 `facts` 数组可读，就恢复其中的事实对象做语义计分；Schema 是否完整合法继续单列。这样不会因为一个 status 枚举写错，就把整题事实全部吞掉。

纠正后，固定 8 字符邻域和完整当前窗口处于同一表现区间，只相差约 0.30 个百分点。完整窗口召回高 4.17 个百分点，但 precision 低 9.24 个百分点；它也出现了只读上下文泄漏或过度推断。当前不能在两者之间拍赢家。

## 停止与复读

- 72/72 正常停止；
- 0 触顶；
- 0 重复事实；
- 0 空题误报；
- 最高单题输出 153 token；
- 当前主要问题是事实覆盖和只读边界，不是“会抽但不会停”。

## 语义盲审

原来 37 个候选裁决全部复用；可恢复口径新找到 20 个候选，只审这 20 个。合计 57 个同题唯一预测中，48 个语义等价、9 个不匹配，待审数为 0。人工裁决只决定事实语义是否等价；Schema、status、speaker、evidence 和停止状态仍分别机械计分。

## 当前结论

这轮只能筛掉“只给目标区明显更弱”这个方向；固定小邻域与完整窗口没有拉开。更重要的是，这 72 次全部使用同一个既有 C2 LoRA，只改了考试输入，并不是三套方案分别微调后的结果。

当前完整窗口还去掉了 P3 历史请求里模型可见的题号，是一份无题号的干净重渲染。三臂内部可以公平比较，但它不是历史 C0 的逐字复现，不能把 P3 的 0.894 与本轮数值直接当成同口径升降。

本轮没有调用 API，没有训练或修改 LoRA，没有改三臂输入、gold、旧 R01 失败票、默认或现役指针。本票只封存零训练预筛，不能替代后续三套独立 LoRA 的端到端对照。

来源：Codex
