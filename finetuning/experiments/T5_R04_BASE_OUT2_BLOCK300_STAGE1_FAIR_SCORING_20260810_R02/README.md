# BLOCK300 Stage1 公平评分 R02

✅ 这包只修评分尺子，不改已经跑完的22条答案，也不运行模型。

完整段八题旧评分没有把证据跳号或乱序当成机械淘汰条件，所以 BLOCK300 也改用同一口径：证据必须是非空列表，而且每个 ID 都要属于本块允许表；升序、连续和最小充分继续作为合同准确性诊断，但不阻断事实语义盲审。

BLOCK300 的117条预测全部原样保留。7条顺序／连续性违约不得排序、补号、删除或去重；跨块重复也全部留在预测分母，一对一最多奖励一次TP，多出的自然成为FP。

完整段八题仍只复用旧结果：29 TP、81 predictions、85 Gold、F1 `0.3493975903614458`。BLOCK300 只有合法语义F1达到 `0.3693975903614458`，才允许控制窗另开后续工单。

当前状态是 `PREPARED_NOT_SCORED`。没有运行正式 pre/final，也没有模型、训练或新请求。

来源：Codex
