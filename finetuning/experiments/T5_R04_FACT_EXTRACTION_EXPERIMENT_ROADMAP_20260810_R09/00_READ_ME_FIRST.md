# 事实抽取实验总路牌 R09｜完整自然责任段保留

✅ 责任块粗筛已经收口：约 60 字档机械退出，没有调用模型；约 300 字档只跑了八题 Stage1，合法语义 F1 为 `0.13861386138613863`，低于预注册门槛 `0.3693975903614458`，不补全 24 题；完整自然责任段继续保留。

完整段实际为每题 `620—923` 个 Unicode 字符，中位数 `760`。冻结八题的旧结果 F1 为 `0.3493975903614458`。这轮不会因为 300 字档失败而改题，也不会再开 `150 / 300 / 450` 细筛。

当前临时 Demo 结构是：**未微调本地 4B＋目标左右每边最多 180 个 Unicode 字符的只读背景＋OUT2-IDLIST＋完整自然责任段**。它只说明当前本机 REAL24 Demo 暂时怎么组合，不是训练、生产或商用 API 的晋级结论。

TRAIN96 不改：48 条 `628—1061` 字的长责任段与当前选中的 `620—923` 字完整段高度重合，覆盖了主要区间，但没有覆盖 `620—627`；48 条 `53—230` 字的短责任段继续教稀疏和短输出，两头都保留。`300—599` 字的空档现在不构成必须补课的理由，本轮不改教材、不训练。

下一项只登记一个候选：`RULE`，也就是在正文范围、输出形式和责任段都固定后，只比较极简规则与增加一两条原子规则。R02 原路线把它排在 READ、OUT 固定之后；它可以先做零训练输入对照，所以是当前最小的格式合同／事实准确性候选。这里没有准备包、执行票或运行权。

机器登记看 [DECISION_REGISTRY.json](DECISION_REGISTRY.json)，结果证据看 [TARGET_BLOCK_RESULT_TICKET.json](TARGET_BLOCK_RESULT_TICKET.json)。R08 及全部旧件保持原字节。

来源：Codex
