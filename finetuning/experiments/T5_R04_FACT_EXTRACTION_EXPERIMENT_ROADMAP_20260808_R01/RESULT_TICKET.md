# 事实抽取实验总路牌 R01 结果票

状态：`PASS_ROADMAP_R01_GOVERNANCE_ONLY_HARD_STOP`

✅ 总路牌已建立，当前只完成治理和计划封装，没有运行任何新实验。

## 登记结果

- 实验臂总数：66
- 独立 LoRA：14
- 无需独立新增 LoRA 的输入／推理格：43
- 完整章专项：9
- 已跑：7
- 未跑：34
- 暂停：23
- 淘汰：2

“已跑”只表示存在可核结果票，不表示已经成为生产赢家。

“无需独立新增 LoRA 的输入／推理格”只表示这一格自己不训练新 adapter。多数格子读取现有 checkpoint；条件式 2×2 也可以读取同家族训练臂以后产出的 checkpoint，不能误读为这一类所有格子都永远共用一个模型。

## 已知结果复核

| 结果 | 现场值 | 结论 |
|---|---:|---|
| A_FULL DEV24 semantic F1 | 76.92% | 与 M1 原票一致 |
| C2_FULL DEV24 semantic F1 | 89.36% | 与 M1 原票一致 |
| D_RANGE DEV24 semantic F1 | 80.43% | 与 M1 原票一致 |
| E_UNIT_QUOTE DEV24 semantic F1 | 86.60% | 与 M1 原票一致 |
| P3 C0 | 0.894 | 极简对照 |
| P3 八规则 C1 | 0.622 | 当前写法淘汰 |
| P3 任务目的 C2 | 0.891 | 与 C0 打平，暂停 |
| P3 确认前态 C4 | 0.825 | 2 案泄漏，当前写法淘汰 |
| Z00／Z01 | 编号臂多报 46、只多覆盖 1、F1 精确低 4.121763 点，约 4.12 点 | 只登记 marker burden 诊断，不判 C2 失败 |
| P4 | 历史封票登记模型推理 0 次 | 只有 preflight 和 toy splitter；当前 `AGENTS.md` 已相对 P4 锁漂移，不再声称可递归复验闭合 |

没有发现历史结果票之间存在需要二选一的数值冲突。施工初稿对 Z00／Z01 的两位小数舍入有误；现已按机器指标改为精确 4.121763、约 4.12。P4 另有一处上游上下文漂移：历史结果仍可引用。P4 verifier 曾被误调用并在 `P4-LOCAL-AGENTS` 硬停；旧重跑没有完成，P4 没有重封；本次定点修订没有再次调用。

## 旧验证器误调用账

这轮施工期间实际发生过三次误调用，不能再写成“从未运行”：

- P4 verifier 被调用，停在 `AssertionError: P4-LOCAL-AGENTS`；P4 文件未见字节变化，没有重封；
- S-02 verifier 被调用并短暂刷新最终回执时间，随后按原字节恢复；当前 SHA 已回到 `1fc19f75e994aa21920e7715c769af8d4a976433e08e0177df5c1b67a2a2f1fb`；
- P3 verifier 被调用；P3 manifest 与最终回执现场 SHA 仍分别是 `d61161cbb731e1156771530b711b403fca0072c9f44e62b8ac3a5ce7b4a5e47d` 和 `193270900494da47a9e23b386ad2c0f9113905196028a6e06ae689452cbb1e93`，未见字节变化。

“旧票当前字节完整”与“调用动作发生过”分开登记。本次定点修订没有再次调用这些旧 verifier。

## 三条路线

1. 输入／推理格：格子本身不新训 LoRA；多数固定 C2_FULL update72，条件式 2×2 可读取同家族新 LoRA；
2. 新训练线：Semantic Core、本地 facts-first F、后续 evidence-first，以及通过推理门后才允许开的 2×2；
3. 完整章专项：P4-SYNTH-PILOT6 的 WRNW 六臂和 metadata 三臂。

完整章不再阻塞短片段实验，也不再承担所有路线的总前置条件。

跨家族禁止做全因子大组合；家族内部可以运行事前登记的小矩阵，例如 WRNW 2×3 和条件式 2×2。

## 终审修正记录

独立只读初审曾硬停初稿。当前 revision 保留并修正了这些问题：

- Z00／Z01 初稿的两位小数舍入错误；
- facts-first 与 evidence-first 被混成一条 F 路线，并出现不存在 Stage B checkpoint 的先决循环；
- 新 LoRA 父模型身份为空；
- 家族赢家先看 CONFIRM24、再组合后复用同一确认集的污染；
- metadata 三臂没有共同锁定 WRNW read mode 和粒度；
- 漏登“按可观察边界检索 0～2 例”及其泄漏禁令；
- P4 上游 `AGENTS.md` 漂移未披露；
- 输入／推理类别名误导 2×2 的 checkpoint 关系；
- facts-first LoRA 没有零训练 Oracle 门；
- “不做全因子”没有区分跨家族大组合与家族内部小矩阵。

后续复核又发现原 facts-first Oracle 门的指标不可执行：M1 的 `42/42` 是“已经语义命中的事实”条件分母，不能再要求提高 5 个百分点。当前已撤回阈值，状态改为 `NEEDS_METRIC_CONTRACT`；下一张相关工单只能先冻结 scorer、DEV24 全 48 gold 共享分母和 citation 失败归因，不能运行 Oracle 或训练。

本票只有在修正版再次通过原审查者只读复核后才保持 PASS；若复核失败，状态应回退为硬停。

## 队列前三张工单

1. `WO-01`：短片段责任范围三臂预检；
2. `WO-02`：1～2 条原子规则候选冻结；
3. `WO-03`：安全 contrastive pair 候选包。

它们当前只是可施工草案，本票不授权执行。

## 数据边界

- TRAIN24：微型训练和接口教学；
- DEV24：开发筛选，已看过，不再称最终盲考；
- CONFIRM24：未来新建，只给已冻结的 `C0 vs BEST-SHORT-CONTRACT` 最终合同使用一次；任何单一家族都不能先看；
- 扩充训练教材：另立数据池，不和 24＋24 的评测题量混写；
- S-02：74 组、314 行、2783 个事实仍是待 CZ 按候选 SHA 确认的训练排除决定；原权利账仍是未知，训练资格为 0。

## 保护边界

本次路线施工授权范围内，下列业务动作全部为 0：

- 模型／API 调用；
- 训练；
- synthetic 文本生成；
- Production Canonical 生成；
- Notion 写入；
- Git 操作；
- `governance/CURRENT_STATE.json` 修改；
- `finetuning/CURRENT.json` 修改；
- 旧 sealed 或 checkpoint 的净修改。

旧验证器误调用不属于“0 动作”，已在上面的动作账单列。S-02 回执有过瞬时刷新，但已恢复原字节；这不能被写成“从未发生”。

治理指针只读观察如下。这里把“本路线没有修改”和“外部并发发生了变化”分开登记：

- `governance/CURRENT_STATE.json`：本路线开工观察 SHA 为 `1d1fca5302cc1fea62e12a8a5362ab7fb9f6a6b9a0b945f886f27e173ec569bc`；封票前现场观察 SHA 为 `44dfe9670b47fb05958400cd6300b3c0aa01f7932e4df655708c2a13873516f0`。该变化来自外部并发窗口，本路线没有编辑、没有回退；它不作为本工单失败。
- `finetuning/CURRENT.json`：开工与封票前均为 `5abaa79b952532881dfc5e147e7da190471baba386aac263474b51edd414f8dd`，本路线没有修改。

## 停点

本轮在总路牌封版后硬停。下一张工单需要单独授权，不能由这份路牌自动启动。

来源：Codex
