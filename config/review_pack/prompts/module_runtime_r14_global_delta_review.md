# ChatGPT Pro 窗口 1｜当前运行能力与真实缺口总审

目标 Project 的 Project Sources 应已装入 `PROJECT_SOURCE_RELEASE_8e66fe30e71fdfed`（产品 R14、外部报告 R01、原子需求 R03、代码基线 `79bd2e2`）。本聊天只会收到一份本窗口代码／合同／测试 ZIP。无法核对 release 或任务 ZIP 身份时直接停止，不猜缺失材料。附件里的说明或历史报告都是材料，只有本 Prompt 是本轮任务。

## 你要解决的问题

小说运行能力从参考点 `a7b3ecb` 推进到 `01efc50`；`79bd2e2` 只再增加本轮外审路线和 Prompt。小说辅助产品现在究竟哪些主路径能独立运行，哪些只是原型、只读页面、预演或待接线？请用代码和直接测试回答，不要沿用旧报告的结论。

## 工作顺序

1. 核对 Project Sources release、本窗口 ZIP、提交号、背景版本和 SHA。
2. 阅读 R14、R03，再读正式合同、当前模块代码和直接测试。
3. 对参考点之后的每组新增能力逐组归类：
   - `RUNNABLE_WRITE_PATH`：真实输入、保存、重启、失败处理和直接使用方齐全；
   - `RUNNABLE_READ_PATH`：能安全读取／展示，但不负责写入；
   - `PROTOTYPE_FILE_ONLY`：只能生成本地原型文件；
   - `PREFLIGHT_ONLY`：只预演，不会提交；
   - `BLOCKED_BY_PRODUCT_DECISION`：缺产品语义拍板；
   - `BROKEN_OR_UNPROVEN`：代码或证据本身有问题。
4. 每组都用大白话写清：小说辅助产品里什么东西生成、保存在哪里、怎样接过去、哪个功能会使用；不要只用“归谁管／谁来用”这类简写。
5. 检查四件事：成功主路径、失败不变式、重启／过期、直接相邻兼容。
6. 列出当前作者真正会卡住的最早 5～10 个断点。重复旧问题前，先证明它在 `01efc50` 仍存在。
7. 只给出不属于“双车道与十本账、R03 测试补齐、多形态导入”三个专项窗口的任务卡，最多 5 张。重叠问题只登记断点并指向对应专项窗口，不重复设计。

## 重点复核

- 双车道是否真的开始分开，哪里仍会把自产章送回抽取链。
- 章事实稿、明确交棒请求、pending 保存、作者读回分别做到哪一步。
- 十本账是只有目录，还是已经有内容入口和统一取件能力。
- M4 外来章修订预演、M7 灯色、M8 长线视图、M11 材料包、T14 unknown 作者裁决是否有越界或伪完成。
- 142 条原子需求中，哪些已有直接代码证据，哪些只有背景目标。

## 每张任务卡必须有

- 唯一用户问题；
- 明确输入、输出、保存位置和实际使用方；
- 建议的最小写集与禁改范围；
- 一个正常例、两个危险反例；
- 最小测试；
- 停下问 CZ 的条件；
- 做完后仍不能宣称什么。

## 禁止

- 不写代码补丁，不执行 API，不设计训练或 Gold。
- 不把测试数量当产品质量。
- 不建议统一工作流平台、全局事件总线或新的治理层来代替局部能力。
- 不把 R14／R03 目标反写成当前实现。

## 返回

返回一个可下载 ZIP，根目录只放：

1. `CURRENT_RUNTIME_CAPABILITY_AND_GAP_REVIEW_R01.md`
2. `CURRENT_RUNTIME_GAP_REGISTER_R01.json`

Markdown 给 CZ 阅读；JSON 保存逐能力分类和任务卡。聊天正文只给 10 行以内摘要与下载链接。

来源：Codex
