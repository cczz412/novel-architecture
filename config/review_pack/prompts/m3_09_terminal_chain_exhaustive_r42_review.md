# ChatGPT Pro 外审 Prompt｜M3-09 R7～R41 全链穷举与 R42 单次补丁账单

你是独立工程审查人。ZIP 内含：

- R7～R35 的失败家族与上次 R35 根因审查；
- 上次外审后的 R36～R41 公开时间线；
- R41 完整公开胶囊：P0／PR／P1／AS／C0／FR／Terra／PF／P2／P3 的 runner、合同、沙箱、封签和校验器；
- R40 的 401 硬停与 R41 的 22＞12 硬停脱敏票；
- 六个 SHA 绑定的 A5／QEC 程序文件。

这次不要只解释当前 `22 > 12`。请把它当成一次上线前的全链审查：沿真实执行顺序把所有公开代码路径走完，尽可能在下一张正式票之前一次找全挡跑问题。

## 必做工作

### 1. 复核上次外审有没有正确落地

核对 R35 推荐的精确文件加载＋内存 package scaffolding 是否在 R41 正确落地；真实装配复验是否真的覆盖正式 Python 3.12.12、`-B -I`、动态 profile、cwd、六模块加载、3 章、30 题和 60 个请求体。指出任何仍可能复发的导入、模块清理或 Python 环境一致性问题。

### 2. 做一份“全链挡跑清单”

严格沿下面顺序逐段审：

```text
P0 → PR → PF 自检 → P1 → Terra 新鲜证明 → AS
→ 正式无网装配 → C0 → 授权原子消耗
→ 60 次直连请求 → 正式输出
→ Terra 正式证明 → PF 冻结 → P2 评分 → P3 收口
```

每个问题必须写清：

- 所在阶段和准确文件／函数；
- 触发条件；
- 是已证实缺陷、高概率隐患，还是材料不足；
- 会不会直接挡住正式跑；
- 最小修法；
- 哪个现有检查本该发现却没发现。

请特别审查那些前三次请求没覆盖、但第 4～60 次或全部完成后才会运行的路径：usage 汇总、原始回包落盘、attempt ledger 对账、正式输出 schema、Terra 消费、PF 三件套、P2/P3 路径和一次性文件冲突。

### 3. 审 `22 > 12`，不要拍脑袋

R41 第 3 个回包：

- HTTP 200；
- `completed/ANSWER`；
- 22 个编号均为字符串、格式正确、无重复、全部属于 66 个允许候选；
- 唯一违规项是数量超过 12。

请核对请求体、Responses API 载荷、结构化输出声明和 parser：

- 服务端实际上是否收到并支持 `maxItems: 12`；
- 当前字段放置、schema 形状或接口兼容是否有错误；
- MiniMax M3／Agent Plan 返回 22 条是“模型忽略约束”，还是“调用层没有真正启用严格结构化输出”；
- 固定 12 个可空槽位是否真比数组可靠；
- 是否有更小、更可靠、且不改变 12 条判据的做法。

禁止建议提高上限，禁止把 22 条事后截成前 12 条后冒充原生合规。若必须设计确定性的本地归一规则，要明确它是否改变评分口径，需不需要 CZ 拍板。

### 4. 给一张且只有一张 R42 补丁账单

不要按“发现一处修一处”输出。请把所有直接挡跑问题合成一次修改，列出：

- 必改文件；
- 函数／字段级改动；
- 对应负例；
- 哪些文件明确不动；
- 新 revision／seal／cycle／run／授权的恢复条件。

把不挡正式跑的问题单列为“已知瑕疵”，留到 R2 收口后的 T3，不要塞进 R42。

### 5. 设计一个端到端公开彩排

只允许一个总彩排入口。它必须使用与正式段相同的 runner 主路径，但注入不含终验题、金标、正文、密钥的公开假件，并覆盖：

- 60 次调用循环的全部状态转移，但网络用确定性本地 fake transport；
- 合规 ANSWER、ABSTAIN、12 条边界、13／22 条超限、无效编号、重复编号、非 JSON、usage 缺失／变体；
- 正式输出 → Terra → PF → P2/P3 的公开假件消费路径；
- 0 重试、预算对账、一票一跑、残件冲突和 fail-closed。

不要再增加几十个静态检查；目标是用一次真实装配彩排替代“合同互相抄答案”。

### 6. 回答 CZ 最关心的问题

请明确回答：

- 下一次正式票之前，还能从公开材料发现多少个直接挡跑问题？
- 做完你的单次补丁账单和端到端彩排后，是否有合理依据把“还需要的正式重跑次数”收敛为最多 1 次？
- 哪些不确定性无法靠公开彩排消除？

不要承诺数学上不可能的 100% 保证。若不能保证，请给一个诚实的上界、条件和剩余风险，不能只写“视情况而定”。

## 钢线

- Agent Plan＋MiniMax M3；
- 30 题 × A5／QEC 两路线，最多 60 次；
- 温度 0、thinking 关闭、自动重试 0；
- 证据编号最多 12；
- 一票一个 run，首个网络尝试前原子消耗授权；
- 不改终验题、金标、评分判据和路线；
- 不要求 ZIP 外文件；
- 不要求终验题、金标、正文、密钥或正式控制目录；
- ChatGPT 只给建议，不替 CZ 拍板。

## 输出

对话正文先给不超过 20 行的结论，并生成一个可以直接点击下载的 ZIP。ZIP 内至少包含：

- `M3_09_R7_R41_EXHAUSTIVE_REVIEW.md`
- `R42_SINGLE_PATCH_BILL.md`
- `REMAINING_BLOCKER_REGISTER.md`
- `ONE_END_TO_END_PUBLIC_REHEARSAL.md`
- `REVIEW_RECEIPT.json`

请把 ZIP 链接直接发在对话里，不要只贴正文，不要用需要预览后另存的单个 MD。

来源：Codex
