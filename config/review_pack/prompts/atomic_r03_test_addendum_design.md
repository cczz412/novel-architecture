# ChatGPT Pro 窗口 3｜142 条原子需求与六例测试补齐

目标 Project 的 Project Sources 应已装入 `PROJECT_SOURCE_RELEASE_8e66fe30e71fdfed`，其中 `ATOMIC_R03__142_EXPECTATIONS.csv` 是本轮原子需求直接入口。本聊天只收到一份本窗口代码／测试 ZIP，包内含旧 127 条需求的四份六例测试设计。无法核对 release 或 CSV 的 142 个唯一 ID 时停止。旧设计不是当前实现证明；它只是 R02 的测试方案起点。

## 你要解决的问题

把原子需求背景从 R02 的 127 条测试设计，诚实升级到 R03 的 142 条：保留仍有效的 762 个旧小测试，为新增 15 条各设计 6 个小测试，共新增 90 个；同时指出旧测试里哪些因当前双车道、章事实稿或新代码已经需要改口径。

## 新增 15 条

- `AE-X-N01`
- `AE-X-N02`
- `M4-N01`
- `AE-X-N03`
- `M8-N05`
- `M8-N06`
- `AE-M10-N01`
- `AE-M10-N02`
- `AE-M11-N01`
- `AE-AW-N01`
- `AE-AW-N02`
- `AE-AW-N03`
- `AE-AW-N04`
- `M9-N02`
- `M7-N01`

另有 7 条旧需求只新增“适用范围：外来道”标签：`M2-E01`、`M3-E01`、`M3-E03`、`M4-C02`、`M4-C04`、`M6-C01`、`AE-X-C01`。不能再把它们用于证明自产章必须重抽。

## 每条新增需求必须恰好 6 个小测试

每个小测试至少包含：

- 全局唯一 `test_id`，统一使用 `AT5-` 前缀；
- 常见程度 `common／medium／rare`；
- 权重，六例合计 100；
- 输入合同和材料配方；
- 预期输出；
- 机械断言；
- 语义评分口径；
- `0／25／50／75／100` 五档锚点；
- 独立红线；
- 当前代码状态：已覆盖、部分覆盖、缺代码、缺产品决定；
- 需要真实小说、合成材料足够或根本不该使用正文；
- 版本对比字段。

六例要覆盖常见、次常见、少见、危险输入和失败恢复，不能只是同一例换名字。需要真实小说时只写脱敏材料配方和抽样要求，不夹带正文，也不生成 Gold。

## 还要做的对账

1. 检查旧四份设计是否恰好覆盖旧 127 条、每条 6 例、权重 100。
2. 列出旧测试中因 R14 双车道和当前代码变化而需要改口径的项目，但不要重写全部 762 例。
3. 区分三类执行：零 API 机械测试、未来模型语义测试、合同／产品决定未闭合所以暂不可执行。
4. 不把冻结回包 PASS、Schema PASS 或测试文件存在写成真实内容能力。

## 禁止

- 不调用 API，不选择模型，不生成 Gold，不读真实小说正文。
- 不修改 R03，不擅自把四条建议升核心或重配权重。
- 不虚构当前代码已覆盖新增 15 条。
- 不把 90 个新增设计混成一个无法定位失败的大测试；每例必须仍可单独评分。

## 返回

返回一个可下载 ZIP，根目录只放：

1. `ATOMIC_TEST_DESIGN_R03_ADDENDUM.md`
2. `ATOMIC_TEST_DESIGN_R03_ADDENDUM.json`

JSON 必须有 15 个 requirement、90 个唯一 test_id，并通过六例计数和权重合计自查。聊天正文只给 10 行以内摘要与下载链接。

来源：Codex
