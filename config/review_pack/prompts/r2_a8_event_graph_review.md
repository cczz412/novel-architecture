# R2 A8 事件图规划器｜ChatGPT Pro 敌对设计审查

你是小说事实材料管线的高级架构顾问与敌对审查员。你只能提供候选设计和反证，不是项目拍板者，也不是盲评裁判。

## 已知现状

- R2 的目标是：作者提出问题后，系统按需回冻结正文取证，给出带可追溯 source ID 的可用回答。
- 当前 A5 机械基线在 2,500 字档为：完整问题 16／28，必答事实头 78／115；B01 为 0／10、17／52。
- A7 只把“事实义务规划器”替换进 A5，其余题集、正文、索引、排序、装包、评分和 7 档预算保持不变。结果：B01 必答头 17／52→25／52，但完整题仍 0／10；总体完整题 16／28→13／28；B02 9／9→8／9，B03 7／9→5／9。A7 已按预注册回退线砍掉。
- Notion 账序真源 v2 在 2026-07-27 12:05 放行 A8“事件图规划器”。包内 `CURRENT_STATE.json` 是 11:55 的旧机器镜像，只能帮助理解结构，不能否定 12:05 放行。
- A8 尚未编码。外审的任务是把它设计成可证伪的单变量实验，而不是替项目宣布它会成功。

## 请回答的核心问题

1. 用包内 A5/A7 实物解释：为什么“多捞到事实头”仍组不成完整回答？事件图规划器真能补这个缺口，还是只是把 A7 换个名字？
2. 给出 A8 的**最小事件图定义**：节点、边、逐字来源跨度、来源 SHA、开放/不确定状态、关系方向；哪些字段必须禁止，避免读到隐藏答案、金标、人工判词或答案数量。
3. 给出从“作者问题→触发节点/边→生成查询→复用 A5 排序与装包”的确定性流程。明确哪些步骤可纯程序完成，哪些步骤若用模型就会破坏当前单变量。
4. 说明如何让 A8 相对 A5 **只改变规划器／查询生成组件**。正文、30 题、7 档预算、稀疏 2/3-gram 索引、排序/非零融合、装包、评分器、分母和隐藏单元必须冻结。请列一张“允许变化／必须字节一致”表。
5. 设计事件图旁车的 JSON 合同与下游兼容接口。图只能产问题触发的候选窗口，不能把整张图直接打包成答案，也不能把 A7 候选帧当 A8 种子。请给 schema 草案、伪代码和 8～12 条敌对测试。
6. 预注册自动砍线是否合理：
   - 新执行器先重放 A5，7／7 选择投影必须字节一致，否则发跑前硬停；
   - A8 任一总体完整题或必答头低于 A5，或 B02 低于 9／9、19／19，或 B03 低于 7／9、42／44，直接砍；
   - B01 完整题仍 0 且总体没有相对 A5 净收益，不进入后续 A10；
   - 候选登记参考线为总体完整题至少 18／28、B01 至少 1／10、B02/B03 不回退。
   请指出其中是否有分母、看分后调参、局部虚高或不可判的问题，并给更严但不换尺的修正。
7. 识别最可能的假胜路径：例如少产出换低错误、把冷索引当答案、图闭包膨胀、同名实体误合并、跨章串线、用 source ID 合法冒充语义承托、将候选边直接计为命中。
8. 如果 A8 方向不成立，请给一个**仍在 R2 已授权目标内**、比事件图更小的替代单变量；不得建议改分母、放宽完整题口径、直接升默认、恢复 Q3、换供应商或使用 DeepSeek 官方 API。
9. 请对整仓 17 个既有失败的只读归因做边界判断：1 个 routes 状态断言和 16 个 provider policy 冻结 SHA 漂移是否与 A8 无关；哪些证据足以登记“先于 A8 存在”，哪些仍材料不足。不要建议顺手修。

## 证据与输出纪律

- 每个重要结论必须引用包内相对路径、运行编号、函数、票据、读数或 SHA；没有证据就写“材料不足”。
- 每条结论标注：`实物证明`／`证据推断`／`推荐`／`材料不足`。
- 机械召回、source ID 合法、schema PASS、图连通都不等于作者问题答对。
- 不得建议让模型给自己签语义绿票；不得把金标、答案、隐藏来源组或人工判词放进规划器输入。
- ChatGPT 通道已污染，只能做顾问，不能充当本项目盲评接收端。

## 交付形式

请直接回复完整 Markdown 报告，不只给摘要，不把关键证据藏在思考过程。若界面支持附件，再附同文文件 `R2_A8_event_graph_ChatGPT_Pro_review_20260727.md`。

建议结构：

1. 一页结论；
2. A7 为什么失败；
3. A8 是否值得做；
4. 最小事件图合同；
5. 单变量边界表；
6. 查询生成算法与伪代码；
7. 敌对测试；
8. 预注册止损线审查；
9. 假胜与泄漏清单；
10. 17 failed 归因边界；
11. 两天内施工顺序；
12. 证据索引。

来源：Codex
