# ChatGPT Pro 窗口 2｜双车道、章事实稿、十本账与检查交接设计

目标 Project 的 Project Sources 应已装入 `PROJECT_SOURCE_RELEASE_8e66fe30e71fdfed`。本聊天只收到一份本窗口合同／代码／测试 ZIP，包内另有一份旧的“什么功能负责生成、保存、交棒和使用”顾问材料。无法核对 release 时停止。旧顾问材料只是线索，必须用 Project Sources 的 `79bd2e2` 已提交代码重判；其中小说运行代码的实际差量止于 `01efc50`。

## 你要解决的问题

把“外来正文入料”和“小说辅助产品自产章事实稿”画成两条诚实、能恢复的产品路线，并说明十本账、M11 供料、T14 检查、C11 登记、事实确认和下一章规划怎样接上。不要再让自产章回 M2／M3 重抽，也不要把预演文件说成正式交棒。

## 已拍板，不能重问

- 外来材料走 C10；自产章走 C11。
- 自产对象叫“章事实稿”，不是正文工作稿。
- INITIAL 标题由作者确认后进入交棒动作，采用方案 A。
- 水印采用保守版。
- AuthorWorkspace 与 planstore 不是一个物理事务；只能用诚实的 pending／恢复状态，不能宣称跨存储原子。
- M7 查已交棒现行内容；T14 查未交棒章事实稿。
- 十本账归小说辅助产品作者工作区登记，各模块仍管各自账内内容。

## 仍需你设计并标出 CZ 拍板点

1. 章事实稿的正式版本身份和稳定编号由哪个现有对象承接，哪些原型字段能直接复用。
2. 章事实稿交棒后，事实是逐条确认、整包确认，还是“整包提交＋逐条状态”；请比较真实作者体验、改稿、撤回、下游失效和恢复成本，给唯一推荐，但不得替 CZ 冻结。
3. C11／章节账存章事实稿时，渲染正文怎样作为可选出口挂载，不能反过来成为唯一真源。
4. M11 当前事实＋未来计划供料怎样进入章事实稿和 T14，如何防止未来计划冒充已发生事实。
5. T14 检测结果、unknown 作者裁决、full_check 引用和 closeout 怎样分工；`completed` 只能表示判断覆盖完成，不能表示全绿。
6. 十本账目录里哪些账当前已有最小读写切片，哪些只有登记，统一取件窗口还缺什么。
7. `AW_COMMITTED_PLANSTORE_PENDING → HANDOVER_COMPLETE` 怎样幂等恢复，失败后作者看到什么。

T14 还要保留四种作者可读状态：明确缺失且无引文、无法判断且无引文、有相关引文但仍无法判断、正常逐字引文。空字符串或纯空白不能冒充空值；作者自报裁决只能加一层记录，不能改写原始 unknown。

## 输出要求

- 画一张外来道和自产道的总图，明确每一步“生成什么、保存在哪、怎样接、谁使用”。
- 列出当前代码可直接复用、需要补窄适配器、需要新产品决定三类。
- 给出不超过 7 张按依赖排序的施工卡；不要把一个跨存储大事务塞进一张票。
- 每张卡写清输入、输出、保存、使用、失败状态、最小写集、测试、禁改和剩余边界。
- 对十本账只设计当前最小接线，不做统一数据库平台。

## 禁止

- 不把章事实稿塞回旧 `WORK_DRAFT` 正文语义。
- 不伪造 chapter_id、revision、fact_id 或计划卡完整字段。
- 不以空串、当前时间或默认值补缺失的正式身份。
- 不执行 API，不给代码补丁，不改 R14／R03。

## 返回

返回一个可下载 ZIP，根目录只放：

1. `DUAL_LANE_LEDGER_AND_HANDOVER_DESIGN_R01.md`
2. `DUAL_LANE_LEDGER_TASK_CARDS_R01.json`

聊天正文只给 10 行以内摘要与下载链接。

来源：Codex
