# R2 最后一道语义臂＋一次性终验隔离｜ChatGPT Pro 顾问任务

你是小说事实材料管线的高级架构顾问与敌对审查员，不是盲评裁判，也不是终验题作者。

请按包内 `00_ROUTE_MAP.md` 的四层取材地图读材料。当前执行真源只认包内 2026-07-27 17:50 的 `TRUTH_SNAPSHOT_PUBLIC.md`；旧 `CURRENT_STATE.json` 与 QEC 合同里的 14:30／“剩余 2 道”字段都是历史镜像，不能覆盖最新拍板。

## 这次只解决两件事

### A. 推荐最后一道开发语义臂

QEC 已正式判负：2,500 字主档与 A5 同为 16／28 完整题、78／115 必答头，B01 仍为 0／10、17／52。只读归因显示逐题状态取窗执行了 94 次，随后 A5 剩余预算又回填 1,414 次；30 格里 24 格只换顺序，只有 1 格真的换窗且没有增加必答头。

请比较少量合法方案后，**只推荐一个**可以在 1 天内完成的单变量实验。候选方向可以含“逐题选择窗口受到结构性保护”“限制全局剩余回填的支配力”或你从实物推导出的更好设计，但必须满足：

- 唯一变量能一句话说清；不得同时改题面规划、查询集合、稀疏排名、总字符预算、评分器或分母；
- 不能靠事后挑百分比、挑预算档、看隐藏分调阈值；如果需要常数，必须给不依赖开发答案的机械来源；
- 只能看作者公开问题、冻结正文、QEC 已封存的公开计划／排名流／轨迹；不得读取答案、金标、隐藏来源组、答案数量或人工判词；
- 先定义无分预演能机械验证的“主档必须真实换窗”闸，再定义唯一一次开发评分的自动砍线；
- 保留 A5 的 2,500 字总预算，B02／B03 不得用牺牲换 B01 假胜；
- 不恢复已砍路线，不升默认，不新增供应商，不扩失败码。

请给出：单变量合同、控制臂和候选臂的精确差异、跑前冻结清单、无分预演、敌对测试、一次评分的有序止损线、可能产生假胜的路径，以及建议落到哪些现有函数／新旁车，避免大重构。

### B. 审一次性终验隔离合同

M1-02 跑完无论正负，主线三会独立封存同模板新 30 题（3 章各 10 题）并交付封签票；主线一不出题、不改题，只凭封签票 SHA 解封后运行一次。当前仓内只有终验隔离合同和开发执行器，还没有主线一可执行的封签接收／一次性终验总控。

你只审合同，不得生成、猜测或要求查看任何终验题、答案、金标、隐藏来源组或人工判词。请给出最小可执行状态机，至少覆盖：

- 主线三封存件、封签票与主线一接收解封的权限边界；
- 开发路线在终验题冻结前后分别能看到什么；
- 进入终验的路线、参数、Prompt、模型身份、重试规则与预算如何同时冻结；
- 无金标工作区预检、每条路线只跑一次、全部输出封存后才解锁评分；
- 可回答数、关键错误数和每题调用／token／耗时怎样形成可复验成绩单；
- 哪些情况必须在发出第一条终验请求前硬停，哪些情况只登记为单题失败而不重跑整轮。

终验达标线固定为：可回答不少于 15／30，且关键错误不多于 10。你不得改判据、改题数、建议试跑终验题或追加第二轮。

## 证据纪律

- 每个重要判断引用包内相对路径；能引用函数、票据、节点或 SHA 时一并写；
- 每条结论标注“实物证明／证据推断／推荐／材料不足”；
- 机械召回、来源编号合法和 schema PASS 不等于作者问题答对；
- ChatGPT 通道已经污染，只能做顾问，不能当终验接收端或语义裁判；
- 任何顾问意见都要由 Codex 筛选、预注册、真实施工和独立审查后才算进展。

## 回答结构

1. 一页结论；
2. 当前证据能证明什么、不能证明什么；
3. 最后一道单变量的候选比较；
4. 唯一推荐实验合同；
5. 跑前机械闸与敌对测试；
6. 一次评分的止损线；
7. 终验隔离状态机；
8. 终验硬停条件与单题失败条件；
9. 假胜、泄漏和重跑风险清单；
10. 施工顺序与证据索引。

## 交付文件

- 把完整报告写入 `R2_M1_02_M1_03_ChatGPT_Pro_review_20260727.md`；
- 把 Markdown 与本轮生成的其他交付文件一起压成 `R2_M1_02_M1_03_ChatGPT_Pro_review_20260727.zip`；
- 最终消息必须附上这个可下载 ZIP。ZIP 根目录直接放 Markdown，不再套第二层 ZIP；聊天正文可以简短，完整内容以 ZIP 内文件为准。

来源：Codex
