# ChatGPT Pro 外审 Prompt｜M3-09 三天补丁循环根因与最小收口方案

你是独立工程审查人。ZIP 内是一个一次性终验装配链的公开材料：脱敏真源、连续硬停摘要、代表性事故票、R35 公开合同、正式 runner、动态 profile 生成器、回归票与最新启动硬停。

请不要把注意力放在模型答案质量上。正式模型请求至今是 0；真正问题是公开装配链连续三天“修一处、下一层再爆一处”。

请完成下面工作：

1. **判 R35 直接根因。** 解释为什么正式 runner 报 `No module named 'experiments'`。逐项核对：Python `-I`、`PYTHONPATH=""`、cwd、`sys.path.insert(PROJECT)`、顶层 `experiments/` 无 `__init__.py` 的 namespace package、动态 sandbox 对父目录只给 metadata／拒绝 scandir、对代码文件给 exact read。给出证据排序，明确哪些已证实、哪些只是推断。
2. **解释为什么 32/32 回归和 P0→PR→PF→P1→AS→C0 全绿仍没抓到。** 指出当前测试实际验证了什么、没验证什么；尤其检查它是否只做 AST／字符串／路径集合差分，没有在“正式 Python + 正式动态 profile + 正式 import 方法”下执行真实导入。
3. **给最小修复，不要再堆新制度。** 至少比较四种方案：
   - A：runner 用 `importlib.util.spec_from_file_location` 和受控 package scaffolding，从六个 SHA 绑定精确文件加载；
   - B：动态 profile 给 `experiments` 相关目录开放必要的目录读取／子树读取；
   - C：给仓库顶层 `experiments/` 增加 `__init__.py`；
   - D：在授权前把六个已绑定文件复制成封存的、正常 package 形状的只读运行胶囊。

   请选择一个改动最小、不会扩大私卷／正文／密钥读取面、不会修改现役实验代码、可以机械证明的方案。给出应改的文件、函数和伪 diff。
4. **给一个且仅一个前置复验。** 复验必须在授权消耗前，用与正式 runner 完全相同的 Python、`-I`、动态 profile、cwd 和导入方法，真实加载 A5/QEC 所需模块；不得读终验题、金标、正文或密钥内容，不得联网。它通过后才允许新 R36 正式票进入 C0/FR。不要新增十几个检查。
5. **审架构是否过度复杂。** 用大白话说明三天延误为什么发生：是 fail-closed 本身必要，还是生成器／合同／校验器／封签／沙箱的重复真源太多？指出可以删除、合并或改成单一生成源的部分。
6. **给收口路线。** 分成“今天为了把这一次终验跑完的最小 R36”和“R2 收口后 T3 才做的统一程序重构”。不要把长期重构塞进本次正式票。

强制边界：

- 不改判据、不换平台、不换模型、不加预算；
- 30 题 × 2 路线，最多 60 次，0 重试，温度 0，thinking 关闭；
- 不要求查看 ZIP 外文件；
- 不要求终验题、金标、正文、密钥或正式授权实物；
- ChatGPT 只给建议，不是接收端，也不替 CZ 拍板；
- 如果材料不足，请精确点名缺哪一份公开证据，不要泛泛说“需要更多上下文”。

输出要求：

- 对话正文先给不超过 20 行的结论；
- 同时生成一个可直接下载的 ZIP，内含：
  - `M3_09_ROOT_CAUSE_REVIEW.md`：完整分析；
  - `R36_MINIMAL_PATCH_PLAN.md`：最小修补与唯一复验；
  - `ARCHITECTURE_DEBT_FOR_T3.md`：延后到 T3 的简化项；
  - `REVIEW_RECEIPT.json`：你读取的关键文件、判断结论、材料缺口和边界声明。
- ZIP 链接直接发在对话中；不要只贴大段正文，不要生成需要预览后才能另存的单个 MD。

来源：Codex
