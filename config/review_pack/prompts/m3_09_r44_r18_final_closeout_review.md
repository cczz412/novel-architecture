# ChatGPT Pro 外审 Prompt｜M3-09 R44／R16～R18 一次性终局审查

你是独立工程审查人。请先完整解压 ZIP，并把它当成一次“禁止继续逐点试错”的终局
审查。不要只解释 R17 的哈希报错，也不要建议 R19、R20、R21 一轮轮试。

包里有三段材料：

- R21～R43 的完整历史根因审查回包，以及更早的 R35、R7～R41 两轮原始回包；
- R44 R16 已完成 60/60 次真实调用后的公开证据、Terra 与 PF 冻结链；
- R17 一次评分硬停、R18 当前候选的评分器、合同、启动器和三个沙箱。

终验题、真金标内容、正文、密钥、正式模型原始回答没有外发。包内只给固定 SHA、
结构说明和公开程序；不要索要这些私有内容。

## 当前不可改的事实

- R16 已完成 60 次真实调用、0 重试；不能重跑；
- R16 冻结输出、Terra 和 PF 链已提交，只允许只读复用；
- R17 P2 命令已执行一次，在写成绩前因金标的旧版 payload 校验规则不兼容而硬停；
- R17 没有成绩单、没有 P2 commit，P3 没执行；同票禁止重跑；
- R18 只允许修复“读取文件自己声明的 canonical payload 规则”，不得改评分语义；
- R18 当前仍未获正式评分授权、没有 seal、没有 formal runtime；
- 后续仍是 0 模型、0 网络，只允许一次 P2 和一次 P3；
- 不改终验题、金标、模型、路线、阈值、R16/R17。

## 你必须一次完成的工作

### 1. 把历史建议和当前实物对平

从 R21～R43 总审查的六个根因家族出发，判断 R44/R16 已经关闭了哪些问题，
R17/R18 又暴露了哪一个问题。不要复述旧报告，要用当前文件逐项判：

`已关闭 / 当前仍挡跑 / 只剩 live-only / 与本次无关`

### 2. 对 R18 做最终消费者级审查

不要停在静态字符串检查。请沿两条真实启动命令走完整调用链：

```text
P2 launcher
→ R18 wrapper
→ SHA 绑定的 R17 scorer core
→ seal / approval
→ R16 frozen chain
→ gold 文件整体 SHA
→ gold 自声明 payload 规则
→ 30 cells × A5/QEC
→ SCORECARD / RECEIPT / P2 COMMIT
→ P3 launcher
→ P2 三件套复验
→ DISPOSITION / CLOSEOUT / P3 COMMIT
```

逐项检查：

- Python 3.12.12、`-B -I` 和 `sandbox-exec` 下是否能真实加载；
- 所有实际读取是否都在对应沙箱中，包含 metadata／`exists()`／父目录创建；
- R18 动态复用 R17 core 后，所有全局路径和 recovery id 是否真的切到 R18；
- R17 core 里遗留的 `R17_*` schema/status 字符串是否只是标签，还是会导致 seal、
  P2、P3 或下游消费者错绑；
- `canonical_payload_rule` 缺席、类型错误、额外字段、算法变化、排除字段变化和内容
  篡改是否全部 fail-closed；
- 私有 gold 只在 P2 读取一次，P3 是否完全不可达；
- create-only 多文件写入若中途失败，是否会留下不可恢复的半套；
- P2/P3 的 commit、SHA、payload 与下一消费者是否逐字段一致；
- usage、cost、claim aliases、ABSTAIN、multi-ref head、unsupported claim 等真实 R16
  数据形状是否还存在确定性崩溃点；
- 成绩门槛仍是完整支持不少于 15/30 且关键错误不多于 10。

### 3. 做一次“全量挡点表”，禁止挤牙膏

请列出从当前 HOLD 到 P3 完成之间的全部已知挡点。每项必须包含：

```text
阶段
精确文件 / 函数 / 字段
是否必现
公开假件能否证明
最小修复
修后要跑的唯一测试
是否需要新授权
```

如果发现多个问题，请在一张补丁账单里全部列出。不要建议“先修第一个跑跑看”。

### 4. 给明确裁定

只能二选一：

- `GO_R18`：包内公开工程面没有剩余确定性挡点，可以签发一次 P2＋P3 授权；
- `PATCH_ONCE_THEN_GO`：给一张且只有一张补丁账单，修完后通过一个端到端公开假件
  测试即可授权，禁止继续拆成多轮。

如果选择补丁，必须明确哪些文件能改、哪些不能改。不得提出重跑 R16、修改金标、
改阈值、增加模型调用或扩大私有读取。

### 5. 回答 CZ 最关心的问题

用大白话回答：

- 为什么前面会从 R21 一直补到 R18 评分恢复；
- 这次审查后还有多少个“公开工程确定性挡点”；
- 下一次评分恢复最多还需要几次本地正式机会；
- 哪些风险只有私有文件读取时才能暴露，为什么；
- 怎样保证以后不再把每个新发现变成一个新版本号。

## 输出要求

聊天正文先给不超过 20 行的结论，同时生成一个可直接点击下载的 ZIP。ZIP 至少包含：

- `R18_FINAL_READINESS_REVIEW.md`
- `HISTORY_TO_CURRENT_CLOSURE_MATRIX.md`
- `EXHAUSTIVE_P2_P3_BLOCKER_TABLE.md`
- `ONE_PATCH_BILL_OR_GO.md`
- `PUBLIC_END_TO_END_TEST_SPEC.md`
- `REVIEW_RECEIPT.json`

所有判断要引用输入 ZIP 内的相对路径、文件和函数。ZIP 下载链接直接发在对话里；
不要只贴正文，不要只生成需要预览后另存的单个 MD。

来源：Codex
