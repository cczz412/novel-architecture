# ChatGPT Pro 外审 Prompt｜B 链场景情绪卡＋T6 结构链第一轮合同审

你收到的是一次合同设计审查包，不是模型试跑包。请先读包根目录的 `00_READ_ME_FOR_REVIEWER.md` 和 `00_ROUTE_MAP.md`，再按路线地图阅读材料。

## 你要解决的问题

请把两条互相隔离的黄灯辅链改成可本地施工的 v0.2 候选合同：

- B 链：从冻结正文生成画面／情绪旁挂卡；
- C 链：对黄金三章给五项绝对结构读数。

现有 T02《黄金三章：承诺建立与首轮兑现》必须继承，不得从零重造一套重复任务。请明确新五项读数与 T02 每个必交项的关系：`复用`、`去重`、`不属于 C`、`待 CZ 拍`。

## 三个对象不得混合

- SceneUnit 是作者计划侧的场景单元。
- B 情绪卡是对冻结正文的画面／情绪旁挂。
- C 结构卡是对冻结正文的章级结构观察和黄灯建议。

B/C 都不能生成 SceneUnit，不能进入事实底账，也不能成为抽取岗训练数据。B/C 可以共用同一冻结正文和同一张 P3 背景卡，但彼此不能作为对方输入。P3 只帮助消歧，不证明情绪或结构判断。

## 已冻结边界

1. 本轮没有小说正文、真实三章、真实 P3 卡，也不需要你补这些材料。
2. 不调用 API、不联网查小说、不扩大样本、不设计 30 本执行方案。
3. 30 本分析库建成前，C 只出本书绝对读数，不做作品分类、相似度或排名。
4. 没有作者计划侧输入时，禁止判断“正文偏离计划”。
5. 黄灯只能是可忽略的顾问建议，不能改写成红灯错误、事实冲突或生产阻断。
6. 不替 CZ 决定模型、温度、预算、阈值、扩样、升默认或正式开工。
7. 不新增第六项结构读数，也不删除 T02 的证据纪律。
8. 缺资料时写 `材料不足`、`UNJUDGEABLE` 或 `CZ_TO_DECIDE`，不要猜。

## 请回答这 8 件事

1. B 情绪卡 v0.2 的输入、字段、场景切分、锚规则、材料不足、黄灯和禁止动作怎样写。
2. C/T6 v0.2 的五项读数怎样定义单位、分母、来源锚、不可判和黄灯建议。
3. B 的 JSON Schema 怎样拒绝事实链、C 链和 SceneUnit 字段。
4. C 的 JSON Schema 怎样拒绝 B 情绪字段、事实链和 SceneUnit 字段。
5. T02 每个必交项如何与五项读数合并、去重或分工。
6. 三章同源、P3 绑定、A 链就绪、外发权利和 SHA 漂移分别怎样检查。
7. 哪些情况只报警，哪些写不可判，哪些必须在模型调用前硬停。
8. 本地三章试跑的输入清单、逐章报告、成本账、可用性判词和收据怎样设计。

## 回件格式

请生成一个可下载 ZIP。ZIP 根目录必须包含以下 8 份实质文件：

1. `01_B_SCENE_EMOTION_CONTRACT_v0.2.md`
2. `02_B_SCENE_EMOTION_SCHEMA_v0.2.json`
3. `03_C_T6_STRUCTURE_CONTRACT_v0.2.md`
4. `04_C_T6_STRUCTURE_SCHEMA_v0.2.json`
5. `05_T02_TO_T6_MAPPING.md`
6. `06_INPUT_AND_MECHANICAL_GATES.md`
7. `07_THREE_CHAPTER_PILOT_TEMPLATES.md`
8. `08_CONFLICTS_AND_CZ_DECISIONS.md`

另附运输文件 `MANIFEST.json` 和 `SHA256SUMS`；它们不占上面 8 件。

ZIP 除上述 8 份实质文件和 2 份运输文件外，不得增加其他业务文件。成员路径必须是相对路径，禁止绝对路径、`..`、重复成员和符号链接；`SHA256SUMS` 必须覆盖 ZIP 内 8 份实质文件，并能逐项复算。

每一项重要建议都要标明依据的包内文件。Schema 必须是合法 JSON；Markdown 要给完整可替换条文，不要只给摘要。不要输出完整思考过程，只给可审查依据、风险和候选条文。

第二轮输入锁必须登记：三章总清单 SHA、逐章 SHA、真实 P3 卡和来源索引 SHA、A 链就绪票 ID 与 SHA、B/C 合同 SHA、实际 Prompt SHA、外发权利票 ID 与范围。P3 来源身份只能使用相对或脱敏后的稳定路径。

如果材料还不足以定某项，请保留缺口并说明本地下一步需要补什么，不要为了“完整”替 CZ 做语义拍板。

来源：Codex
