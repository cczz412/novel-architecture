# 施工单｜可先施工批_R04归因清点＋R05需求粗整理_20260716

<aside>
✅

2026-07-16 02:03 CZ 已拍「发」放行。两单可并行、互不依赖；全零 API 调用、不动默认指针、不动冻结件、不领新 G 号、禁领 G101。产物回传后停，等 Notion 统一审。

</aside>

## R04 主线施工单（Codex）｜G100 归因清点

- 任务＝对 G100 非同源单臂 8 章双秤读数逐条分型，零调用。
- 输入＝本地 `generated/ab_runs/G100_非同源单臂8章_v1.5规则_v1.6契约_20260715/` 双秤 JSON＋8 份模型消息正文＋冻结 v1.2 金标（SHA 照旧校验，只读不改）。
- 出账口径＝内容秤 62 FN＋23 FP、严格秤 65 FN＋26 FP，每条标主病（可附次病）：
    1. 章型病＝该章信息形态现行规则／契约接不住（写明接不住的是什么形态：过程／知情差／纯对话载信息等）。
    2. 金标口径病＝v1.2 金标选条尺度与 v1.1 精雕不同尺（写明尺度差在哪）。
    3. 欠产出病＝模型该出没出／整段漏收（与选错区分开）。
- 产物＝①逐条清点表（JSON 或 MD，每条＝章号／atom／秤／主病／一句话理由）；②人话报告（三病占比＋逐章小结＋对 T1 切片悬案的证据标注）。带 SHA，回传总台照旧，标「待统一审」。
- 红线＝零 API 调用；不改提示词／规则／契约／金标；不动默认指针；分型是候选判断不是结案，结案在统一审。
- 停点＝清点回传后停。

## R05 支线施工单（Cursor）｜需求文档粗整理＋原料汇编

- 任务＝为结构对撞总表备料，只整理不判定，零调用。
- 输入＝本地 nvm-notion-core-md-tree 核心包快照（真源在 Notion，不迁移不回写）。
- 动作①＝逐页过 NVM 需求文档，出「结构块清单」：每块＝名字／管什么／字段大意／出处页路径。只抄录归纳，不评判接不接得住。
- 动作②＝把路线图页第三节原料清单逐项摘成卡片：名字／一句话定义／样本出处。
- 产物＝两份 MD 清单回传；对撞判定（三色清单）由 Notion 执笔。
- 红线＝不改需求文档本身；不新建结构；只出清单。
- 停点＝两份清单回传后停。