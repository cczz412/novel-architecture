# 真书承重 R03：逐条评审、产品初始化与 C9 取件

本轮完成 R02 留下的三项施工：152 条未绑定事实逐条评审，其中 135 条追加投影、17 条保留；通过产品函数初始化新项目的空发号账；C9 实际取回凡人四账 15 张、无限六账 23 张候选卡。两次 C9 结果相同，投影同操作号重放不改文件。PR 送审、不合并，CCZ-176 不关闭。

授权为 [#317 的 CZ 2026-09-08 06:30 决定](https://github.com/cczz412/novel-architecture/issues/317#issuecomment-5576264064)。工程基线 `main@45d0eb61a89cb843bbce6a2b1f0aa471c4e110c5`，已含 R02／PR #323；本轮依新授权修改初始化代码和测试，R02 原回执保留。跨段 #322、标签组 #314 不在本 PR 写集。

## 输入分母与评审

沿用两书原书前三章及 R02 核对过的单段材料，在新的隔离 AuthorWorkspace 重新走正文接纳、分段、已有抽取导入和本地测试确认；没有真实模型 API。事实 ID、事实文本、逐字引文和原章锚定保持一致。300 总数／182 单段测试确认／58 跨段暂缓／60 内容不支持的四类分母不变。

| 书 | 单段确认 | R02 已绑定 | 本轮评审 | 新增绑定 | 仍保留 | 累计绑定 |
|---|---:|---:|---:|---:|---:|---:|
| 凡人 | 60 | 13 | 47 | 41 | 6 | 54 |
| 无限 | 122 | 17 | 105 | 94 | 11 | 111 |
| 合计 | 182 | 30 | 152 | 135 | 17 | 165 |

辅助 Agent 逐条提出建议，主窗口逐条对照事实、引文和目标后作出本轮决定。135 条含 11 条简介和 124 条描述状态，均为 model 提议、candidate／draft_inferred；没有作者签字，不写 alive。它们主要保存有来源的原句描述，并未把所有内容拆成结构化关系、客观规则或可执行条件。

R02 点名的三组已逐条处置：

| 组 | ID 与处置 |
|---|---|
| 多对象 8 条 | 凡人 f010 归人物家庭，f025 归镇的布局，f033 归门派背景，f047 归马车行程；无限 f001 归车厢场景，f021 归说话者发言，f027／f028 因电影认知来源与前史边界保留。未拆原事实、未复制为多条绑定。 |
| 名称 6 条 | 凡人 f006／f007 归已有韩立卡，f028 归三叔局部身份，f031 归已有门派卡；无限 f003／f038 归同一局部新人身份。局部描述名只用于本书本场景，不补造实名或自动合并。 |
| 说话者规则 12 条 | 无限 f005／f007／f008／f009／f011／f012／f015／f017／f019／f062／f108 保存为张杰发言，f084 保存为詹岚的电影记忆推算；没有升级为客观世界规则。 |

仍保留的 17 条逐项归因：

| 书 | ID | 本轮保留理由 |
|---|---|---|
| 凡人 | f016、f018 | 一句含多个不同归属或制度、计划、动机，未选定不失真的单一绑定。 |
| 凡人 | f024 | 未来旁白，不能让结局在当前章提前生效。 |
| 凡人 | f042、f048、f057 | 群体描述缺合适的可识别目标，不能擅自摊给主角或全门派。 |
| 无限 | f027、f028、f065 | 事实简写弱化了电影记忆或人物解释的来源边界。 |
| 无限 | f029、f043、f057 | 说话者或人物身份尚不够明确；不凭邻近名字猜主体。 |
| 无限 | f034 | 死亡推断被写成结果，事故目标亦待明确。 |
| 无限 | f071、f074 | 本轮未选存亡结果表达方式，暂不写专门存亡动作；f074 还缺稳定人物身份。这不是合同禁止所有死亡事件描述的结论。 |
| 无限 | f075、f100 | 受伤者或受害者身份未明，不补名、不推死亡。 |

无限 f031／f032 采用“已叙述的前史”记录键，章锚表示这段记录首次可得，不表示历史事件发生于该章。梦境、意图、建议和人物说法仍保留原句限定。时态推理能力没有在本轮验收。

## 初始化与投影

产品入口为 `setting_projection.initialize_setting_allocator(workspace, book_id=...)`，调用方显式提供项目内书号，函数内部构造并校验空 plan，书核包含合同要求的完整字段：故事前提空串、题材承诺与结局锚为null、节拍为空，保留draft_inferred来源、UTC登记时间和rev=1，不伪造作者内容。`settingstore` 在既有 planstore 锁内原子写入；已有书号和计数必须一致，重复调用不重置计数。有设定记录却缺发号账时拒绝初始化。绑定工作区的适配层持有 workspace 锁再取得 planstore 锁，防止检查 logical plan 后被同期写入穿透。

这是显式产品初始化入口，未自动接入创建项目界面。AuthorWorkspace 的 logical plan 与实体发号账仍是两个存储面：发现已有 logical plan 时明确拒绝；初始化成功后，统一workspace提交入口也在同一把锁内拒绝再写logical plan。两种初始化顺序只允许一种存储面先取得写入位置，不猜测同步。空的设定账文件按现行合同读为空数组。目录 fsync 失败可能已留下完整合法文件，调用报错后可重试确认；不承诺报错必然未写。

两本新工作区初始化前均没有 plan.json，由上述产品函数首次返回 INITIALIZED，重复返回 ALREADY_INITIALIZED 且文件哈希不变。本地运行脚本没有预写发号账；手写绑定仍按现有入口写入实体绑定文件，本票没有新增绑定编辑界面。

先用原 30 条绑定复现 R02 的卡 ID，再追加本轮绑定，保留已存在卡的身份：

| 书 | 本轮新建卡 | 本轮追加事实 | 本轮跳过 | 其中仍无绑定 |
|---|---:|---:|---:|---:|
| 凡人 | 6 | 35 | 19 | 6 |
| 无限 | 8 | 86 | 28 | 11 |

跳过包含原来已投影的 30 条与本轮保留的 17 条；不把旧绑定再次计成新增成果。两书 `op-r03-reviewed-bindings` 重放回执相同，所有非锁文件哈希不变。

## 真实消费者结果

本轮调用公开组装入口 `unified_retrieval_composition.run_current_retrieval`，经真实 reader 和 adapter 生成 C9 结果；没有 monkeypatch，也没有直接把候选卡塞进返回值。每本对其全部非空设定账建立 HARD 需求，逐张核对返回内容与实体候选卡一致。

| 书 | 人物 | 地点 | 物品 | 势力 | 体系 | 世界规则 | C9 结果 |
|---|---:|---:|---:|---:|---:|---:|---|
| 凡人 | 8 | 4 | 1 | 2 | 0 | 0 | READY，4 类需求满足，15 张候选卡 |
| 无限 | 11 | 4 | 5 | 1 | 1 | 1 | READY，6 类需求满足，23 张候选卡 |

凡人两类空账仍为空，没有为凑数补造设定。C9 第二次完整取件与第一次一致。这里只验证宽预算下指定卡取回，没有验证搜索召回、预算取舍或模型推理；体系和部分物品默认定义仍可能只含名称与事实引用，不能据卡数量声称原事实全部性质已经展开。

| 环节 | 本轮结果 | 未验证部分 |
|---|---|---|
| C9 完整组装 | 两书实际调用并取回上述卡 | 自动找需要的卡、精确时点推理、紧预算筛选 |
| 门3页面 | 本轮未运行 | 页面交互与展示 |
| 门4／门5 | 可供 C9 取件的候选材料已验证 | 目标卡生成、实际续写 |
| 章末结算 | 本轮未运行 | 封条、结算回写、失效传播 |
| 主 AI／外部插件 | 本轮未启动 | 语义理解、外部权限、标签组 v2 受众裁剪 |

## 验证与本地证据

首轮定向初始化／投影测试 99 passed；C9 组装测试文件 7 passed。五个改动代码／测试文件 Ruff 通过，diff 检查通过。首轮全量 `uv run --locked pytest -q`：4224 passed、902 skipped、40 deselected、1 xfailed、179 subtests passed，252.67 秒，退出 0（本地日志 `TEMP/ccz176_r03_full_pytest.txt`）。

两书 182 条逐条通过：C4 原章区间回取等于原引文及其哈希，事实文本和 ID 不变；原始文件哈希未变，接纳的 C1 等于完整派生前三章。

正文、事实、引文、绑定和运行结果只在本地：`local/ccz176_r03_review/` 保存逐条建议和主窗口决定，`local/ccz176_r03_run/` 保存新工作区、初始化／投影／C9 回执及核验。原 R02 证据仍在 `TEMP/ccz176_real_books_20260907/`。本 PR 只含产品初始化代码、对应测试和本回执；不含这些本地材料。

旧 W1／CCZ-172 的“全量逐条落账评审与至少一个实际消费者”欠缺由本回执承接；17 条保留项和未验证消费者仍如上列明。初始化改动和本回执均须经 PR 审阅，不能用本地运行成绩替代合并或整体验收。

审查补件：逻辑计划与实体发号账的后续写入冲突、空文件误拒已修复。修前3条反例失败，修后初始化／投影／C9／plan工作区共118项定向通过；新增同项目并发创建用例确认只有一个存储面成功。为封住后续直接commit绕路，写集增加统一workspace提交入口的一道拒绝检查；不改变其他逻辑键，也不增加自动同步能力。最新组合全量测试结果在本PR审查回执中另列，不能用首轮计数冒充新版本。

归位条件：R03验收及后续报告归位获批后，回执目标为 `reports/ledger_load_bearing_r03/RECEIPT_R03_real_books.md`，保留本PR固定版本回链；本次按#317批准位置保留，不自行搬迁或复制本机正文。

来源：Codex
