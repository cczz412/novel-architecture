# 00 任务与边界

身份：Codex。归属方向：CCZ-57。本轮只是 L1 API 可拆性小探针，不算 CCZ-57 正式开工，不替代 CCZ-84，不评分，不升 Gold。

## 结论口径

精确模型 X 在 4 条人工合成材料上的原始表现为：哪些明确事实被抽出，哪些遗漏，哪些出现主体／状态／关系／因果错误，哪些回复格式不合法。该结果只属于 L1 可拆性探索，不证明模型整体可用，不计分，不升 Gold，不替代 CCZ-84，也不代表 CCZ-57 正式开工或完成。

## 自检

1. 根目录 AGENTS.md 已读。真值是 GitHub `cczz412/novel-architecture`。
2. GitHub main HEAD：`8872887690fd018474a69979016b428f2c2faf56`。干净工作树：`wt-ccz57-l1-probe`。本轮不改仓库代码。
3. 正式入口：
   - 上工 `governance/START_HERE.md`
   - 机器状态 `governance/CURRENT_STATE.json`
   - 指针 `governance/current_pointers.json`
   - 模型调用档案 `config/model_call_profiles/registry.json`
   - Agent Plan 名单 `config/providers/volcengine_agent_plan.json`
   - 事实抽取合同 `novel-fact-extraction-v2.1`
   - 产品试跑抽取通道 `novel-mvp/mvp/extract.py` 的 `call_json`（arkcli +chat）
4. 其他窗口工作树只检查，不清理、不复用。主 clone 在别人的脏分支上，本轮不用。
5. CZ 点名的两个模型：`doubao-seed-2-1-turbo-260628`，以及请求名 `deepseek-v4-flash-modelhub`（期望响应 `deepseek-v4-flash-260425`）。
6. 闸门：不伪造 C13 的 execute ticket。Agent Plan 发网前必须实时目录核对、精确模型 ID、arkcli profile 凭证、清掉环境 Key、重试 0。运输走 arkcli +chat。
7. 实时目录已核对。Doubao-Seed-2.1-Turbo 身份一致，完成 4 次主采样。DeepSeek-V4-Flash 冻结请求名不在实时目录，按停损停止，未采样、未换号。

## 材料

四段由本窗口全新编写，全部虚构。没有用 local 正文、新书池、Gold、CCZ-84 题面。

## 调用上限

计划 8 次。每题每模型一次。自动重试 0。目录核对比主采样先做，不算题目次数。不为凑满 8 次突破停损。

## 外发

只向 CZ 交本地候选。不写 Linear / GitHub / Slack / Notion。
