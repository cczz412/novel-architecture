# 已有问答卡与报告索引

先看问答卡和调查收件卡，不默认深读报告。

`novel-architecture` 中不要把 Linear 的 `CCZ-60` 票面当作问答卡正文。先读 GitHub `main` 上的 `agent-playbook` 目录和相符卡片，再读 `CCZ-64` 中对应的调查方向。两边已经覆盖同一对象、范围和时间时，停止调查。

| 本轮问题 | 相符问答卡 | CCZ-64／其他覆盖台账对应项 | 已有短答案 | 范围与时间 | 证据身份 | 仍未回答 | 后续证据指针 | 是否需要深读一段原报告 |
|---|---|---|---|---|---|---|---|---|
| 一次抽取还是迭代补漏 | `clean-output-can-still-miss`、`evaluation-needs-layers`、`verifier-diagnoses-not-writes` | CCZ-64 已查“抽取／评测／记忆”；GAP-P2-02 明确最终净收益走本地消融 | 已知高准确不等于少漏抽；要反看源文覆盖；诊断和修复分开，修复不能自动写真值 | 中文小说抽取产品边界，2026-08 当前 | 已拍加固＋外部先验 | 迭代抽取相对一次抽取，在什么任务、模型和成本下有可复现净收益 | SI-002、EKB `CLM-DR-EVAL-02-K13`／`03-U01` | 是；旧卡只把 gleaning／补漏列为 U 级候选，缺当前可复现实证 |
| 成熟开源 Agent 怎样纠错 | `checker-needs-coverage`、`retries-are-real-attempts` | CCZ-64 已查 SI-008 Agent 工具／执行包／权限 | 已知工具门控、服务端游标、生成前守卫、覆盖回执和有限缺料回取 | 相邻软件工程 Agent，2026-08 当前 | 外部先验 | 现役 GitHub 项目的真实循环实现、状态持久化、停止条件、失败恢复和公开缺陷 | SI-008 | 是；旧调查重点是工具暴露和生成前守卫，不是生成后的验证—修复循环 |
| Codex／Claude Code／Cursor 等怎样回头改 | `verifier-diagnoses-not-writes`、EKB UX 页 | CCZ-64 GAP-P2-04 仍是“Agent 断线／重试／覆盖回执原型”轻项 | 已知权限在模型外、恢复要区分是否提交、checkpoint 不等于万能 Undo | 当前商业 Coding Agent 与大厂工具 | 外部先验，部分旧来源已过期或超时 | 各产品公开可观察的测试、终端反馈、diff、checkpoint、沙箱、用户复核和重试机制；哪些属于编排而非模型聪明 | EKB `CLM-DR-UX-03-K04`、`K07`、`K10` | 是；需要按研究当日官方资料更新 |
| 生产 Agent 为何会越修越错 | `retries-are-real-attempts`、`checker-needs-coverage` | CCZ-64 GAP-P2-04 | 已知重试是新尝试、外部副作用不可假装撤销、检查不能只回裸 OK | 生产工作流与 Agent 工程 | 外部先验 | 自我确认偏误、无限循环、重复副作用、上下文污染、截断和状态损坏的公开案例与工程停损 | SI-008、EKB UX | 是；旧材料有原则，没有系统整理当前生产复盘 |
| 哪类错误该用思考、补抽、程序或独立验真 | `change-one-variable`、`evaluation-needs-layers`、`verifier-diagnoses-not-writes` | CCZ-64 GAP-P2-02 最终选择必须本地消融 | 已知格式、语义、证据、覆盖必须分层；同一模型自检不能直接写回 | 长文结构化抽取与小说事实候选 | 已拍边界＋外部候选 | 按错误类型分配修复机制的外部实证；尤其遗漏、跨句因果、指代、模态、动机／伏笔候选 | SI-002、EKB 抽取页 | 是；外部只提供候选路由，产品选择仍留给本地验证 |

## 当前状态回读

- GitHub `main` 核对时间与提交：2026-08-27，`8872887690fd018474a69979016b428f2c2faf56`；通过 GitHub API 读取现行 `START_HERE`、Agent 问答卡、SI-008 和 EKB R01。
- Linear 核对时间、对象与访问结果：2026-08-27，只读回读 CCZ-64 成功；状态 Todo。与本题直接相关的是已查 SI-008、GAP-P2-02 本地消融、GAP-P2-04 轻量原型。
- 本地材料只算线索的说明：本地 `novel-architecture/` clone 为 `a6244cd...` 且有其他窗口修改，落后于远端 main；本批未读取它作为当前工程真值，也未改动它。

## 卡片已经足够回答的内容

- 工具权限、管线游标、生成前守卫和缺料回取上限已经由 SI-008 覆盖，不重新调查。
- 高准确不代表少漏抽；输出后必须反看源文覆盖，这一点已有问答卡，不重新调查概念。
- 验真负责诊断，修复另起候选，不能自动覆盖真值，这一点已有产品边界，不交给 Deep Research 重拍。
- 一次实验只能改一个变量；未来本地对照必须冻结模型、输入、Prompt、解码、Schema 和评分器。

## 最小停止判断

- [x] 读的是 GitHub 问答卡正文，不只是 `CCZ-60` 票面；
- [x] 已回读覆盖台账中与本题相符的一项，没有要求台账逐张抄出所有问答卡；
- [x] 已把 SI-008 重复内容停掉；下面五份 Prompt 只询问旧卡仍未回答的增量。

来源：Codex
