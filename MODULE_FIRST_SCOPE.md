# M1～M11 模块优先工作说明

## 现在真正要做什么

当前目标不是上线，也不是把生产治理一次做满。

先让 M1～M11 每个模块分别达到“可用原型”：输入能进、核心流程能跑、输出能交给下游、关键失败不会悄悄写坏数据，并有一个本地示例证明它能工作。

最终希望做到：无论什么类型的小说进入系统，都能完成导入、切分、抽取、作者审查、查询、体检、规划、概览、导出和上下文装包。等模块基础能力稳定后，再用真实用户场景检验，包括用户修改剧情、调用插件、遇到材料不足或前后冲突等情况。

## 工作顺序

1. 每个模块先只管自己的输入、核心行为和输出，做到可独立运行。
2. 每完成一批模块，再做一次跨模块兼容检查，不在每个小步骤后反复总审。
3. 兼容问题退回对应模块修，不新造一个治理层替模块兜底。
4. M1～M11 都达到可用原型，接口也对齐后，再串完整端到端流程。
5. 真实用户调查、插件体系、剧情修改场景和大规模功能扩展放在后续阶段；它们会继续改变设计，现在不做过早冻结。

## “模块可用”怎么判断

一个模块至少要有：

- 明确输入和输出；
- 一条真实可运行的主路径；
- 最重要的失败路径会明确拒绝或返回未完成，不静默猜测；
- 一个贴近用户的本地示例或定向测试；
- 已知限制写清楚，但不为了未来所有可能性提前建制度。

这不等于 Production Ready，也不要求现在就有完整 UI、扩容、计费、训练、Gold、SLA 或生产部署。

## 防跑偏规则

- 不能推动某个模块更接近可用的任务，不做。
- 只有输出新账、SOP、回执或治理字段，且没有新增模块能力的任务，不做。
- 没有新输入、新失败或新行为时，不重复跑同一轮审查。
- 每张施工票只回答：哪个模块、缺哪项用户能力、改哪些实现、怎样证明可用。
- 兼容检查是阶段性关卡，不是日常主工作。

## 本分支的身份

本分支保存当前完整因果链，供云端和人工监督工作有没有跑偏。它不是准备整体合并到 `main` 的发布分支。正式代码应走只包含实际模块改动的干净 PR；TEMP 证据继续留在监督分支。

来源：Codex
