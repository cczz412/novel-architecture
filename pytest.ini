[pytest]
testpaths = tests
norecursedirs = .git .venv TEMP runs reports outbox
markers =
    historical_replay: 已退出日常验收、只在复制好的历史工作副本中回放
    local_evidence: 依赖本机候选证据或封存运行件；干净副本缺件时明确跳过
    v02: 抽取工序重设计 v0.2 新区测试；与 LEGACY 旧测试分区
