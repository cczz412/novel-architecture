# 外发审查 Prompt｜用 R13 纠错合同并出无歧义稿（2026-08-15）

身份：**合同歧义消除题。** 给已经存有现行 R13 的 ChatGPT 项目用。ZIP 里只有合同和字段表，**不要**再要背景板、不要叠 R11／R12。回包是问题清单＋候选合同稿，不自动改 R13、不自动改设计稿、不写代码。

## 复制这段给 ChatGPT

```
请对照你已持有的共同背景板 R13，审查我刚上传的合同 ZIP。这不是深度行业调查，也不是让你重写背景板或另起一套产品。不要写代码，不要发明新的产品功能。

先核对版本：
- 背景板封面必须是 NOVEL_ARCH_SHARED_CONTEXT_CORE_MATERIALS_20260814_R13。若是 R09／R10／R11／R12／草案／只有摘要，先停下来说明，不要硬审。
- ZIP 里不应有背景板。若包里出现 R13／R12 全文，当运输错误记下，仍以你已持有的 R13 为准。
- 权威：R13 赢。已落合同（C1–C4、C6、C7 v0）描述的是代码现状，可能过时。设计稿字段表是人读候选，还不是正式合同。冲突记「合同／字段表过时」，不要反过来改 R13。

请列出你实际读到的：R13 成员页（至少 00、01、02、03、05、07），以及 ZIP 里每个文件名。缺页就停。

产品身份（当约束，不要推翻去另起一套产品）：
- 给网文作者用的导写＋核对编辑助手，不代写正文。红线：不给写作模型、不生成书稿成文。
- 主体用户：已过黄金三章、正在 3～20 章的网文作者。黄金三章是独立工作台，满意后再移交进项目。
- 作者界面叫大纲，内部叫规划账。CLI 不是作者主界面。
- 计划 ≠ 已发生。句子样子可以一样，身份不能混（PE- 是计划，F- 是已确认事实）。
- 设计稿写了不等于已经实现。C7 v0 只是出题切片，落在 plan_latest.json，不是整本 plan.json。
- 收费冻结不动。ADD-043／044 仍开口，不要当已拍。ADD-045～048 不吸收。
- 暗稿「默认已发生、未描写、仍是事实」（K3）已拍，不要改掉。开口只剩：算不算签过字、能不能开下一章。不要把 slot_status=closed 写成关章或开下一章。
- 外写／写作区新稿入库默认只对照；作者选「以这篇为准」才交棒。不要写「默认正文赢自动更新」或「粘贴入库与移交同一下」。
- 投影可改；进真值必须作者确认。不要写回「投影只读／禁止在卡上改」。
- 收工 ≠ 关章。对账可选。无正文收工不跑对账。

本轮要你做两件事，都要做：

【一、纠错】找出 ZIP 里的已落合同、字段表，和 R13 不一致、彼此打架、或同一句能读出两种施工的地方。每一条必须能指到：文件名＋短引，以及 R13 哪一页（或另一份合同／字段表）。分清：已验证（能引原文）vs 你的推论。

盯这些已知容易脏的点：
- C7 v0 跟规划账 plan.json 是不是被写成同一份东西
- C4 事实账跟规划账有没有字段互冒
- ARCHITECTURE「概览／投影只读、禁止在卡上改出第二真源」vs R13「投影可改、进真值须确认」
- 存储稿 R03 已改「入库不自动交棒」，M8／停点／写作区有没有漏改或自相矛盾
- slot_status 的 planned／partial／handed_over／closed／dropped 各表示什么，谁有权改
- 对账边六态 vs 关章六道门 vs 收工完成态，三套词有没有焊在一起
- C1 v0 只有章节正文，导入六架／黄金三章工作台还没进合同
- 英文字段名是否漏进了作者界面口径

【二、做出没有歧义的合同稿】在纠错之后，写出可以给施工用的候选合同。要求：
- 每个字段只能有一种读法。必填／可空／枚举值／谁写谁读／落哪份文件，都写死。
- 每个字段必须标注来源：STORAGE R03 原文／R13 已拍／已落合同 C*／你的新建议。新建议不得写进「必填」；单独放「开放／候选」节。
- 禁止发明新前缀、新真值表、新 API、新收费档。人物账／灵感账本轮不要整本新开，只在规划账引用处写「指向 CH-／IDEA-，字段归宿主稿」。
- 界面人话和英文字段名分开写。作者看见的词用中文。

本轮合同稿的优先级（按这个顺序写，写不完就停在下一档之前，不要平均用力）：

1. 🔥 规划账存储合同（plan.json ＋流水）。这是本包主菜。把 STORAGE R03 第 1 节十三类对象翻成正式合同：公共字段、书核、卷、章槽、场、计划事件、故事线、伏笔、挂件、引脚、必写承接、选择记录、槽位映射、对账边、顶层骨架。写清：入库默认只对照、选「以这篇为准」才追加 handover_parts；truth_bearing 只挂五类对象；plan ≠ fact。
2. 规划账和已落合同怎么接线：C4 只读事实、C7 是出题快照不是账本、C6 是体检投影。给一张「谁落盘、谁过期、谁能写」表。
3. C5 概览卡候选合同（从 REVIEW 稿 §2 翻），必须带：投影可改、进真值须确认、过期作废、不另开第二真源。
4. 若还有篇幅：标出 C1 v1／C8／C9 各缺哪几句才能无歧义开工；不要整本写完。

输出用中文，给人看。不要空话，不要重写背景板，不要当拍板。

先给一页总览：
- 你读到的 R13 版本＋ZIP 文件清单是否对得上
- 最严重的 8～12 条纠错（每条一行：一句话＋文件名）
- 本轮你实际写出了哪几份合同稿，哪几份只标了缺口

然后分两大部分：

A. 纠错清单。每条统一格式：
- 编号（A1、A2…）
- 严重度：硬伤 / 高风险 / 含糊 / 过时合同 / 真空
- 位置：文件名＋小节；对照 R13 哪一页
- 原文短引
- 为什么会让后面的人各做各的
- 合同里应改成哪一句（一句话，不要扩成方案）

B. 候选合同稿。每份合同包含：
- 合同名＋建议版本号（规划账建议从 plan-v2 候选起，不要假装已经落地）
- 一句话用途
- 发／收模块
- 落盘文件（或「不落盘」）
- 字段表（英文名 / 类型 / 必填 / 人话 / 枚举 / 来源标注）
- 一条合法 JSON 示例（可用《万鬼伏藏》样例改，不要另编一本新书世界观）
- 明确禁止（至少写：不能冒充事实账、不能自动交棒、不能把收工写成关章）
- 开放问题（只列 ZIP 或 R13 已经开口的，不要替我们拍）

数量宁缺毋滥。同类问题合并。纠错目标大约 15～40 条，不是 200 条碎句。

最后加一节「扫描覆盖缺口」：哪些文件你读得浅、哪些对象你没写成合同。

这是合同歧义消除，不是产品拍板，也不是授权改 R13 或代码。
```
