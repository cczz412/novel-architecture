# 同步与变更协议

状态：**R07 现行流程页；管版本、同步、冲突和各方用法，不提供施工或训练权限。**

## 这次 R07 怎样过渡

R07 直接派生自不可变更的 R06，只改 2026-08-12 逐原子裁决覆盖的地方。CZ 已在本窗口明确把顺序定为“先在本地做出来，然后再上传 Notion”，因此这次走：

1. 从 R06 建独立的本地 R07 目录，R06 不动；
2. 按 29 个原子 ID 逐条并页，改判、退回和挂账互不连带；
3. 做本地机械验收，成员 SHA 在清单中暂记 `PENDING`；
4. 上传前逐字回读 2026-08-12 账序，按实际裁决修订本地页；
5. 在 R06 的 Notion 父版下或总库约定位置创建 R07 页面组；
6. Notion 写完后逐页回读，将本地内容与 Notion 版对平；
7. 重算成员 SHA、核心 SHA、整包 SHA 和 manifest SHA，回填清单与验收回执。

2026-08-12 裁决原先漏记。CZ 在本窗口明确要求创建并修复后，已于 21:18 把 R03“账序粘贴稿 v3”按 29 个原子补入账序真源，并回读确认。R07 随后完成本地逐项对平、Notion 页面创建、逐页回读与最终 SHA 登记，现已发布为共同产品说明。

## 一个版本必须带什么

- 包名、版本号、状态和入口页；
- 核心八页 00～07；
- 08 验证、市场与风险登记页；
- 用户数据权利与安全政策页；
- 产品语义债清单；
- 项目接入参考页；
- 机器清单与构建验收回执；
- 父版本身份与四个 SHA；
- 裁定截止、证据截止、变更影响表和开放问题；
- 清楚写明“共同背景板不等于执行权”。

## N14｜市场数字保鲜规则

状态：`APPROVED_DIRECTION`（依据账序 2026-08-12 21:18 条）。

市场、竞品、价格、规模、采用率、收入、成本、产能和转化等数字，每一项都要保存来源快照，并附五件套：数据日期；主体与统计单位；指标种类；可以推导什么、不能推导什么；下次复核日。五件套不全的数字只能放在调查材料或待补区，不能进入产品结论、定价判断或对外话术。数字变化只刷新 08 页，不在核心页散落复制；任何对外引用前都要检查复核日和原始来源。

## N24｜辅助页、原子裁决与语义债

状态：`APPROVED_DIRECTION`（依据账序 2026-08-12 21:18 条）。

- [验证、市场与风险登记页](08_VALIDATION_MARKET_AND_RISK_REGISTER.md)承载竞品、数字、价格试验、验证证据和双计分卡详表。
- [用户数据权利与安全政策](USER_DATA_RIGHTS_AND_SECURITY_POLICY.md)承载权限、权利、删除、训练和安全边界。
- 核心八页只保留稳定结论、开放问题和到辅助页的路由，不复制易变明细。
- 候选统一按原子 ID 和五状态登记：`CANDIDATE`、`READY_TO_DECIDE`、`APPROVED_DIRECTION`、`DEFERRED`、`REJECTED`。
- CZ 按原子逐条裁决，不整包批准；相邻条目不得自动连带。
- R07 随包附[产品语义债清单](PRODUCT_SEMANTIC_DEBT_REGISTER.md)，分清已稳定原则、仅方向、首个作者可用验收前必须解决三档。

这些载体只说明理解成熟度和前置问题，不替代施工任务、验收票或上线许可。

## 什么时候需要出新版本

出现以下任一情况，就建立新版本，不在已发布版本里静默改字：

- CZ 新拍或改拍会改变产品语义；
- 正式合同或结果票让现有描述失真；
- 现行版出现会误导执行的冲突；
- 新增或撤销背景板成员；
- 易变登记页的更新影响核心结论。

纯文字修复、不改语义时可以出补丁版，但仍要重算 SHA、登记差异；不得原位改写已发布父版。

## 更新与裁决规则

- 账序保存拍板原句，是裁定证据真源；背景板负责解释，不代替账序。
- 裁决按原子 ID 处理。`APPROVED_DIRECTION` 只进入语义方向；`DEFERRED` 留开放问题；`REJECTED` 从正文摘除并保留追源；未出现的原子维持原状态。
- 与父版冲突时，00 页用“旧句／旧读法 → 新句”登记原子 ID、裁决依据和影响页。
- 题 15、题 16 继续沿 R06 挂账，除非账序明确逐题改判。
- 语义方向不自动生成数据库、接口、工程任务、模型调用、训练或生产许可。
- 字段、表结构和接口细节由 T3 与后续施工合同另行冻结。

## 变更分级

| 级别 | 例子 | 处理 |
|---|---|---|
| 裁决变更 | 用户、真值边界、核心流程、权限边界改变 | 新版＋账序依据＋逐页影响表＋完整机械验收 |
| 合同对齐 | 正式字段或验收合同冻结后修正背景板描述 | 新版或补丁版，注明正式合同与影响范围 |
| 易变资料刷新 | 竞品、市场、价格和验证数字更新 | 只改 08 页；如改变核心结论再升版 |
| 文字修复 | 错别字、坏链接、不改语义 | 补丁版＋SHA 重算＋修复记录 |

## 各方怎样使用

### Notion

- 已发布版负责给人阅读；账序继续保存拍板原句。
- R07 已完成账序对平、Notion 建页、逐页回读与 SHA 登记；R06 作为不可变父版保留。
- Notion 页与本地包发生语义差异时，不默改任一侧，先回账序和 CZ 指令确定对应原子。

### 本地仓库

- R06 目录只读；本地 R07 是独立派生目录。
- 本工单不修改根 `AGENTS.md`、任何 CURRENT 指针或正式治理状态；这些本地机器入口是否切换另走明确工单。
- 不碰 finetuning 与 t5_r04 的任何文件，不运行训练或模型 API。

### ChatGPT、外部模型和新窗口

- 外部建议只进候选池，不能拍板。
- 每次按任务所需发送最小材料，并先过权利与外发许可；本次没有外发授权。
- 新窗口先读 00，再读相关页；数字回 08，拍板回账序，执行现状回正式合同和结果票。

## 机械验收

本地建版阶段至少检查：

- 00～08、政策页、语义债页、参考页、manifest 和 receipt 全部存在；
- 每份 Markdown 的末行有来源；
- 入口页能到全部成员，本地相对链接全部可达；
- JSON 可解析；
- 29 个原子 ID 与 R03 一一对应，N10-A／N10-B 为远期挂账，其余按方向映射；
- N19-B 的规划槽位序列、实际章节序列、映射与四种偏差处理完整；
- 题 15／16 仍挂账；
- R06 文件未被改动；
- 除本工单明确授权的 R07 本地目录、账序补登记和 Notion R07 页面外，没有修改 Git、CURRENT、finetuning、t5_r04，也没有调用模型、训练或外发；
- manifest 父版 SHA 与 R06 封存值一致，成员 SHA 在 Notion 对平前保持 `PENDING`，对平后回填最终值。

Notion 建版后已经补齐：页面数量、逐页回读结果、最终成员 SHA、核心 SHA、整包 SHA、manifest SHA 和 Notion 页面 ID。

## 当前 R07 身份

- R07 本地目录与 Notion 页面组都已建立，R06 作为不可变父版保留。
- R07 已完成账序逐条对平、Notion 逐页回读与最终 SHA 登记，是现行共同产品说明。
- 现行身份只负责共同产品理解，不产生施工、训练、外发、模型调用、生产晋升或上线授权。
- 后续若某个原子被改判、退回或继续挂账，只修该原子与它在 00 页的影响记录，不连带其余条目。

来源：Codex
