# 项目接入参考

状态：**R10 草案页；替拍条目待 CZ 复核；CZ 改判只摘对应段落不连带。保存接入方案，不是当前路由或执行合同。**

## 这页保留什么

为了让新窗口和未来自动化找到共同背景板，曾设计三层入口：外层 `README.md`、机器可读 `CURRENT.json`、仓库 `AGENTS.md` 阅读规则。当前只启用 R06 已有的 `AGENTS.md` 最小入口，没有第二个 shared-context CURRENT。

## R10 草案本次建版结果（2026-08-13）

- [本地 R10 草案入口](00_READ_ME_FIRST.md)已创建，第三册挂账本 ADD-021～035 逐条吸收完成（替拍待复核）＋ADD-036／ADD-037 亲拍已吸收；成员 SHA 转正时再重算；
- **本版不推 Notion**（`NOT_PLANNED_UNTIL_CZ_APPROVES`）：Notion 现行版＝R09（同步已完成）；CZ 复核通过、本包去 `_DRAFT` 后缀转正后再议同步；
- 本任务不修改根 [AGENTS.md](../../../AGENTS.md)，不创建 `README.md` 或 `CURRENT.json`，也不改治理 CURRENT；额外授权仅一条：回填第三册挂账本状态列；
- 复核前任何机器指针、路由或消费者都**不应**指向本草案当正式版用。

## R08 建版结果（历史）

- [本地 R08 入口](00_READ_ME_FIRST.md)已创建，挂账本 ADD-001～017 逐条吸收完成，成员 SHA 已由脚本重算登记；
- **Notion 同步 pending**：R08 页面组尚未创建，Notion 总入口当前仍指向 R07；同步完成前，本地 R08 是最新共同产品说明；
- 本任务不修改根 [AGENTS.md](../../../AGENTS.md)，不创建 `README.md` 或 `CURRENT.json`，也不改治理 CURRENT；额外授权仅一条：回填挂账本状态列；
- 根 `AGENTS.md` 与本地机器指针仍未切换；如需切换，必须另有明确工单，并同时核对所有直接消费者。

## R07 建版结果（历史）

- 本地 R07 入口已创建并对平账序，Notion 建版、逐页回读与最终 SHA 登记均已完成；
- R07 建版时 Notion 总入口已指向 R07，R01～R06 均保持原样。

## 以后启用稳定指针时

- 在 shared-context 外层建 `README.md` 指向当前版本；
- 建 `CURRENT.json` 登记版本号、入口、核心 SHA 和整包 SHA；
- 每次发新版同步更新两者并在账序留证；
- 发布前跑 06 页机械验收；
- 不得让 `CURRENT.json` 与 `AGENTS.md` 各指一处。

## 本地与 Notion 的关系

- R07 依 CZ 明确指令先做本地建版再创建 Notion 页面，两侧已对平；R08 未单独推、随 R09 一次性同步完成；
- **R09 仍是现行正式版**（本地与 Notion 一致）；本 R10 草案是替拍先行版，CZ 复核通过后才接任；R06、R07、R08 作为不可变历史版保留；
- 技术状态仍以本地正式合同、结果票和运行指针为准，背景板只做解读。

来源：Codex（R07 底版）＋Cursor／Fable 5 Max（R08、R09 吸收组装；R10 草案替拍组装）
