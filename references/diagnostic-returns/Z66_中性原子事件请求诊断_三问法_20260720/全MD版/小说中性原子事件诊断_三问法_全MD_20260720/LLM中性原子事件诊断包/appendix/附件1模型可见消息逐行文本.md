# 附件1模型可见消息逐行文本

## system

`S001` 你只做证据约束的中性事件摘取，不做类型判断。只输出合法 JSON。

## user

`U001` # Z00l 中性事件提取 Prompt v1.0
`U002` 
`U003` 你只看当前一章，不得使用本章之后的知识。
`U004` 
`U005` 下方“冻结证据目录”按原文顺序覆盖当前章全文，短引之间有少量重叠。你的工作只有一件事：找出以后可能被继续读取的**原子事件**，并为每条选择真正支持它的证据 ID。程序会把 ID 确定性展开成章号＋10～25 字连续原文短引。
`U006` 
`U007` 不要判断事件属于哪一种结构类型，不要输出 A／B／C／D，不要输出状态、事实等级、关联编号或类型专属字段。分类由下一阶段主控统一处理。
`U008` 
`U009` 找事件时依次检查：已经发生的变化、正文说出口的后续安排、正文公开留下的待回答事项、正文明确说明的可重复机制。这里只做清点，不贴标签。
`U010` 
`U011` 每条必须满足：
`U012` 
`U013` - 一条只写一个事实头，不把同一场景的多件事捆成一句。
`U014` - `event` 只写正文在本章支持的中性事实，不写“这是承诺／规则／触发器”等分类判断。
`U015` - `anchors` 只交冻结目录里的 `anchor_id`；一条可选多个 ID，但每个都必须真正支持该事件。
`U016` - 宁可保留有锚的边界事件，也不能为了让结果干净而漏掉真实变化或明确安排。
`U017` - 普通动作、气氛描写、无后续读取价值的场景复述不凑数。
`U018` - 事件 ID 必须按原文顺序从 `EV-C0003-01` 连续编号。
`U019` 
`U020` 只输出一个 JSON 对象，不要代码围栏，不要解释：
`U021` 
`U022` ```json
`U023` {
`U024`   "schema_version": "z-event-v1",
`U025`   "chapter": 3,
`U026`   "events": [
`U027`     {
`U028`       "event_id": "EV-C0003-01",
`U029`       "event": "本章明确发生或说出口的一件中性事实",
`U030`       "anchors": [{"anchor_id": "E0001"}]
`U031`     }
`U032`   ]
`U033` }
`U034` ```
`U035` 
`U036` 当前章号：`3`
`U037` 
`U038` 当前章文件名：`0003_第3章_梅丽莎（第一更求推荐票）.txt`
`U039` 
`U040` 冻结证据目录：
`U041` 
`U042` ```json
`U043` [本行共 12078 字；完整内容见附件1原始请求体与冻结证据目录文件]
`U044` ```
`U045` 
`U046` 来源：Codex
