# 收件区

这个目录主要解决“材料和报告从哪里来、有没有被改过、该归到哪一批”的问题。

- `manifests/`：机器清单，记录原件路径、文件数、大小和 SHA-256。
- `raw/`：以后需要长期保留的新入站原件。旧 X/Y/W 批暂不搬，只做路径登记。

入站原件不在这里润色。需要整理的版本另放 `work/` 或 `reports/`，这样原件和加工件不会混。

来源：Codex
