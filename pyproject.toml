[project]
name = "novel-architecture-pipeline"
version = "0.0.0"
requires-python = ">=3.11,<3.12"
dependencies = []

[dependency-groups]
dev = [
  "pytest==9.0.2",
  "ruff==0.15.22",
]

[tool.uv]
package = false
