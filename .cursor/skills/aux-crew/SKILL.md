---
name: aux-crew
description: >
  审计员团（不对称助手团）：不挑主会话模型（Fable／Opus／Grok 均可），主 agent 自己从系统
  提示识别当前模型，无需用户申报。主 agent 负责判断与综合，派 Grok 4.6 extra high 子 agent
  （model 固定 cursor-grok-4.6-xhigh，禁止加 -fast）做只读取证、穷举、核对、
  对照组数据收集与红队挑刺。按任务形状自动启用，与主会话是什么模型无关：
  方案比较／对照组分析（哪组更好）、
  架构与链路审计（工作区链路是否通畅、维护是否方便）、背景板／共同背景／产品设计文档审查、
  制定方针路线图，以及需要读多个分散文件或核对多条事实的调查。
  普通问答、解释概念、单文件小改、闲聊不启用。
  用户说「上审计员／派人／取证」强制启用；说「别派人／轻量点／直接答」本轮禁用。
  用户明说「cursor 团队」走 composer-team，明说「自检／探路」走 scout-check，本 skill 让位。
  执行类施工默认起草工单交 Codex，不在本 skill 内直接施工。
---

# 审计员团（aux-crew）

一句话：**你（主脑）出判断，审计员出腿。** 便宜助手做能被抽查验证的体力活；取舍、结论、方针必须主 agent 自己下，不许外包。

## 触发三档（解决「忘了喊 / 误触发」）

| 档 | 什么情况 | 行为 |
| --- | --- | --- |
| 自动上 | 任务形状命中：方案／对照组比较、架构链路审计、背景板或产品文档审查、定方针路线图、需要读 ≥3 个分散文件或核对 ≥5 条事实的调查 | 不等口令，直接派；派前对用户带一句「派 N 个审计员查 X」，不必等确认 |
| 默认不上 | 普通问答、概念解释、单文件小改、闲聊、看一眼某个文件 | 主 agent 直接答，零派发 |
| 口头开关 | 「上审计员／派人／取证」 | 强制启用 |
| | 「别派人／轻量点／直接答」 | 本轮禁用，即使任务很重也只自己干 |

误触保险（就算判断错了也不贵）：默认**单轮、1～3 个、只读**；发现任务其实很小就不派，中途也可随时收手。

## 分工铁律（不对称的核心）

子 agent（Grok）**只做**这四类——共同点是产出可以被抽查验证：

1. **取证**：读指定文件/目录，回报事实 + `文件:行` 引用
2. **穷举**：列出所有 X（消费者、引用点、配置项、断链）
3. **核对**：逐条验证一份清单（路径存在吗、数字对得上吗、两处说法一致吗）
4. **红队**：对主 agent 的草稿结论找反例、找漏项（产出当线索，不当裁决）

子 agent **禁止做**的——这些留给主脑：

- ❌ 「你觉得 A 方案还是 B 方案好」——判断题不外包
- ❌ 写最终结论、定方针、改正式文件
- ❌ 开放式漫游（「看看这个仓库有什么问题」）——必须给窄范围和输出格式

## 派发规矩

- `model` 每次显式写 **`cursor-grok-4.6-xhigh`**（口语「4.6 extra high」「4.6 xhigh」；不写 = 用了别的模型，违规）
- ❌ 禁止写成 `cursor-grok-4.6-xhigh-fast`（带 fast，CZ 2026-08-13 钉不用）、`cursor-grok-4.5-high`（旧钉，已作废）、漏写 `model`
- 只读探查用 `explore` 类型，多步核对用 `generalPurpose`；能并行就一条消息里一起派
- 子 agent 看不到对话历史：prompt 里写清「你只负责 X」+ 文件路径 + 输出格式 + 行数上限；让它自己去读文件，不要整段粘贴长文档进 prompt
- 每条回报必须带引用（`文件:行` 或原文短摘），无引用的结论视为传闻
- 🔥 **主 agent 必须抽查承重事实**：子 agent 说「X 文件里写了 Y」，凡是这个结论会影响你的判断，就自己开文件看一眼再用
- 预算：每波 ≤4 个，一般 ≤2 波；额度充足但综合质量优先，别派一堆读不过来

角色派发模板见 [references/dispatch-templates.md](references/dispatch-templates.md)。

## 输出骨架（给 CZ 的回报）

```text
【结论】主脑自己的判断，一段说清
【证据】审计员回报要点（已抽查的标注 ✓）
【分歧/风险】审计员之间或与我判断不一致的地方，怎么裁的
【下一步】要不要出工单给 Codex；要的话工单草稿放哪
```

## 与其它 skill 的边界

- **触发只看任务形状，不看主会话模型**。主 agent 从系统提示自识别模型，CZ 切换模型不用申报
- 主会话本身就是 Grok 时照常启用：审计员的价值在「并行跑腿 + 强制引用 + 主脑抽查」这套流程，同模型也成立；分工铁律不变（判断仍归主 agent）
- 本 skill 是重设计／审计任务的**默认协作方式**，取代旧的同模型满编打法
- 用户明说「cursor 团队」「自检」「探路」→ 仍走那两个 skill，它们的模型继承规则自管
- 施工（改代码、跑训练、动正式资产）不是本 skill 的活，默认产出「给 Codex 的工单」收尾

## 硬禁令

1. 把判断题（哪个更好、要不要改）整包丢给子 agent = 违规
2. 未抽查就把子 agent 的承重结论写进【结论】= 违规
3. 漏写 `model` 或写错 slug = 违规，重派
4. 拿本 skill 当施工队直接改正式文件 = 违规，改出工单
5. 闲聊/小问题硬派人凑仪式感 = 违规，直接答

来源：CZ 2026-08-09（主脑换 Fable/Opus，子 agent 走 Grok，额度充足；设计审计讨论在 Cursor，规范执行交 Codex）。CZ 2026-08-13 改钉：子 agent 改用 4.6 extra high，slug＝`cursor-grok-4.6-xhigh`，**不用 fast**；旧钉 `cursor-grok-4.5-high` 与误写 `cursor-grok-4.6-xhigh-fast` 一律作废。
