# CZ 当前两项直接授权｜2026-08-18 R01

## CZ 明字

> 这不是有这些方案吗？你直接用就可以了呀。你觉得，就是你看一下，你觉得靠谱就直接用就可以了呀。你这总停，就是你尽量去想办法，然后呢，就可以了。只有说什么两条路，比如说未来就两条路都差不多，就是往左走还是往右走，这种再问我。

## 总控按最近明确对象收窄解释

这句话直接承接总控上一条列出的两项完整方案，因此视为以下两项的 CZ 明字授权，不要求 CZ 再复制长模板：

1. **A｜C11 RESTORE 双动作正式化**
   - `RESTORE_LINEAGE`：允许作者把已合法提交的历史 revision 按原内容追加成新 revision；
   - `REACTIVATE_CURRENT`：当前消费资格必须另行核验 origin material 当前 C10 仍为 `CONFIRMED + CHAPTER`；
   - 允许在原 C11 正式化票写集内修改 C11 action、Schema、validator、fixtures，并跑定向回归；
   - 不修改 C10、产品、R13，不提交或推送 Git。
2. **T03｜自动候选真实能力方案 B**
   - 从约 1600 本新书池按冻结规则机械选择 4 本不同题材、每本 1 章；
   - 盲参考先于模型输出，身份 `formal_gold=false`，不写正式 Gold 或作者真值；
   - 豆包 2.1 Turbo 非思考走现役 Agent Plan；经实时目录核验，DeepSeek 请求 ID `deepseek-v4-flash-260801` 的输出身份为 `deepseek-v4-flash-ga-260731`，因此也走 Agent Plan，不走 DeepSeek 官方付费渠道；同卷配对共 8 次，自动／人工 retry 均为 0；
   - 诊断判断线：span recall ≥80%、FULL bundle recall ≥70%、限定词保留 ≥90%、普通负控误触 ≤5%、危险误连=0；
   - 两模型均使用已持有的 Agent Plan，新增按量付费上限人民币 0 元；调用前核实套餐状态、请求 ID、输出身份和预计套餐用量，任一不符即 0 调用停点；
   - 不改正式 C3/C4、MVP、R13、Git、Notion，不训练、不上传、不进生产。

## 以后怎么问 CZ

- 总控能从本地证据和顾问草稿里得到唯一明显优选时，直接在既有权限和费用帽内施工；
- 两条路线真实代价和收益接近、会改变正式产品语义、或必须扩大上面明写的写集／费用／材料／模型时，再请 CZ 选择；
- ChatGPT、Notion、Pro 建议仍只是顾问草稿，必须先由总控核本地证据；本记录不把任意未来建议自动变成权限。

来源：Codex
